---
phase: 03-animation-webgl
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - components/cards/PortfolioCard.tsx
  - components/ui/StatBlock.tsx
  - app/portfolio/PortfolioGrid.tsx
autonomous: true
requirements: [ANIM-04, ANIM-05, ANIM-06, ANIM-09]
must_haves:
  truths:
    - "Portfolio cards lift 4px with shadow transition on hover"
    - "Portfolio filter reorders cards with smooth FLIP animation"
    - "Stat block numbers animate from 0 to target value on scroll-enter"
    - "Section content fades up on scroll-enter across the site"
    - "All Framer Motion animations respect prefers-reduced-motion"
  artifacts:
    - path: "components/cards/PortfolioCard.tsx"
      provides: "Card with Framer Motion hover-lift"
      contains: "motion/react"
    - path: "components/ui/StatBlock.tsx"
      provides: "Stat block with animated number counter"
      contains: "CountUp"
    - path: "app/portfolio/PortfolioGrid.tsx"
      provides: "Portfolio grid with FLIP filter animation"
      contains: "layout"
  key_links:
    - from: "components/ui/StatBlock.tsx"
      to: "components/motion/CountUp.tsx"
      via: "import and render"
      pattern: "import.*CountUp.*from.*@/components/motion/CountUp"
    - from: "components/cards/PortfolioCard.tsx"
      to: "motion/react"
      via: "motion.div hover animation"
      pattern: "from.*motion/react"
    - from: "app/portfolio/PortfolioGrid.tsx"
      to: "motion/react"
      via: "layout prop for FLIP"
      pattern: "layout"
---

<objective>
Wire Framer Motion animations into existing components: hover-lift on PortfolioCard, animated number counters on StatBlock, FLIP filter animation on PortfolioGrid, and ScrollReveal integration for page sections.

Purpose: These are the Framer Motion-domain animations (per D-01). They add micro-interactions and polish to the static cards, stats, and filter grid built in Phase 2.

Output: 3 modified files — PortfolioCard.tsx (hover-lift), StatBlock.tsx (counter), PortfolioGrid.tsx (FLIP + scroll reveal)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-animation-webgl/03-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/03-animation-webgl/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — CountUp component props -->
interface CountUpProps {
  target: number;
  suffix?: string;
  prefix?: string;
  duration?: number;  // default 1.5s
  decimals?: number;  // default 0
  className?: string;
}

<!-- From Plan 01 — ScrollReveal component props -->
interface ScrollRevealProps {
  children: ReactNode;
  className?: string;
  delay?: number;     // default 0
  as?: 'div' | 'section'; // default 'div'
}

<!-- From Plan 01 — useReducedMotion hook -->
export function useReducedMotion(): boolean;

<!-- Current PortfolioCard.tsx -->
// Server Component — no "use client"
interface PortfolioCardProps {
  slug: string;
  title: string;
  category: string;
  subcategory: string;
  excerpt: string;
  heroImage: string;
  heroImageAlt: string;
  className?: string;
}

<!-- Current StatBlock.tsx -->
// Server Component — no "use client"
interface StatBlockProps extends ComponentProps<"div"> {
  value: string;  // e.g. "8,000+" or "5 min"
  label: string;
  dark?: boolean;
}

<!-- Current PortfolioGrid.tsx -->
// Already "use client" — has filter state
interface PortfolioGridProps {
  caseStudies: CaseStudyFrontmatter[];
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hover-lift on PortfolioCard and FLIP filter on PortfolioGrid</name>
  <files>components/cards/PortfolioCard.tsx, app/portfolio/PortfolioGrid.tsx</files>
  <read_first>
    - components/cards/PortfolioCard.tsx (full file — currently Server Component with CSS hover)
    - app/portfolio/PortfolioGrid.tsx (full file — already "use client" with filter state)
    - app/page.tsx (lines 189-200 — PortfolioCard used in homepage teaser grid too)
    - app/portfolio/[slug]/page.tsx (lines 96-110, 161-175 — PortfolioCard used in up-next sections)
    - DESIGN.md (lines 166-176 — card hover spec: lift 4px, shadow-md to shadow-lg, 320ms, ease-out)
    - .planning/research/PITFALLS.md (Pitfall 7: never GSAP + Framer on same element — this is Framer-only, safe)
  </read_first>
  <action>
    **PortfolioCard.tsx modification** — Add Framer Motion hover-lift (ANIM-05):

    Currently a Server Component with CSS `transition-shadow duration-normal hover:shadow-md`. Must become a Client Component with Framer Motion:

    1. Add `"use client"` directive at top
    2. Import `{ motion } from "motion/react"` and `{ useReducedMotion } from "@/lib/useReducedMotion"`
    3. Replace the outer `<Link>` with a wrapper approach: keep the `<Link>` for navigation but wrap the card content in `<motion.div>`. The `<Link>` stays as the outer element for accessibility (keyboard navigation, crawlability). The `motion.div` wraps the visible card styling.
       - Actually, better approach: wrap the entire `<Link>` in a `motion.div` that handles hover animation. The `<Link>` stays as a child with all its existing classes.
       - OR: use `motion(Link)` to create an animated Link. Framer Motion v12 supports `motion()` factory for custom components.
       - Simplest correct approach: Keep `<Link>` as-is. Wrap in `<motion.div>` with hover animations. Move the visual classes (border, rounded, bg, shadow) to the motion.div. Link becomes `className="block"` only.
    4. The `motion.div` gets:
       - `whileHover={{ y: -4, boxShadow: "0 24px 60px rgba(10, 11, 13, 0.12)" }}` (translateY -4px + shadow-lg from design tokens)
       - `transition={{ duration: 0.32, ease: [0.16, 1, 0.3, 1] }}` (320ms, ease-out)
       - Keep existing classes: `border border-border-light rounded-lg bg-surface`
       - Remove the CSS `transition-shadow duration-normal hover:shadow-md` — Framer handles this now
    5. Keep the existing image `group-hover:scale-[1.02]` CSS transition — this is a CSS-only transform on a child element, not conflicting with the parent's Framer transform
    6. If `useReducedMotion()` returns true: render with no `motion.div` wrapper — plain div with same classes, no hover animation
    7. Comment at top: `// Framer Motion domain — hover-lift micro-interaction (D-01, ANIM-05)`

    **PortfolioGrid.tsx modification** — Add FLIP filter animation (ANIM-06):

    Already a `"use client"` component with filter state. Add Framer Motion `layout` prop for FLIP:

    1. Import `{ motion, AnimatePresence } from "motion/react"` and `{ useReducedMotion } from "@/lib/useReducedMotion"`
    2. Import `{ LayoutGroup } from "motion/react"` for coordinated layout animations
    3. Wrap the card grid `<div>` in `<LayoutGroup>`
    4. Wrap each `<PortfolioCard>` in a `<motion.div>` with:
       - `key={cs.slug}` (stable key for FLIP identity)
       - `layout` prop (enables FLIP position animation)
       - `initial={{ opacity: 0, scale: 0.95 }}`
       - `animate={{ opacity: 1, scale: 1 }}`
       - `exit={{ opacity: 0, scale: 0.95 }}`
       - `transition={{ duration: 0.32, ease: [0.16, 1, 0.3, 1] }}` (320ms, ease-out)
    5. Wrap the card list in `<AnimatePresence mode="popLayout">` so exiting cards animate out before new layout is calculated
    6. If `useReducedMotion()` returns true: skip AnimatePresence/motion wrappers — render PortfolioCard directly with no animation
    7. Comment: `// Framer Motion domain — FLIP filter animation (D-01, ANIM-06)`
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PortfolioCard.tsx line 1 is `"use client"`
    - PortfolioCard.tsx contains `import.*motion.*from.*motion/react`
    - PortfolioCard.tsx contains `whileHover` with `y: -4`
    - PortfolioCard.tsx contains `useReducedMotion`
    - PortfolioCard.tsx does NOT import from `gsap` or `@/lib/gsap`
    - PortfolioGrid.tsx contains `import.*motion.*AnimatePresence.*from.*motion/react`
    - PortfolioGrid.tsx contains `layout` prop on a motion element
    - PortfolioGrid.tsx contains `useReducedMotion`
    - PortfolioGrid.tsx does NOT import from `gsap` or `@/lib/gsap`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Portfolio cards lift 4px on hover with shadow transition (ANIM-05). Filter reorders cards with smooth FLIP animation (ANIM-06). Both respect reduced motion. All in Framer Motion domain (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Animated stat counters on StatBlock and ScrollReveal integration</name>
  <files>components/ui/StatBlock.tsx, app/page.tsx</files>
  <read_first>
    - components/ui/StatBlock.tsx (full file — currently Server Component showing static value)
    - components/motion/CountUp.tsx (from Plan 01 — verify props interface)
    - components/motion/ScrollReveal.tsx (from Plan 01 — verify props interface)
    - components/sections/HeroDark.tsx (lines 73-93 — renders StatBlock in stats row)
    - app/page.tsx (lines 158-163 — StatBlock used in homepage featured case study section)
    - DESIGN.md (lines 140-141 — number counters animate from 0 on enter)
  </read_first>
  <action>
    **StatBlock.tsx modification** — Add animated number counter (ANIM-04):

    Currently a Server Component rendering static `{value}` text. The `value` prop is a string like "8", "500+", "0.92", "5 min", "40 days", "24/7".

    Strategy: Make StatBlock a Client Component that parses the numeric portion and uses CountUp for the number, preserving any suffix text:

    1. Add `"use client"` directive at top
    2. Import `{ CountUp } from "@/components/motion/CountUp"` and `{ useReducedMotion } from "@/lib/useReducedMotion"`
    3. Parse the `value` string to extract: numeric part, prefix, suffix. Examples:
       - "8" → target: 8, suffix: "", prefix: ""
       - "500+" → target: 500, suffix: "+"
       - "0.92" → target: 0.92, decimals: 2
       - "5 min" → target: 5, suffix: " min"
       - "40 days" → target: 40, suffix: " days"
       - "24/7" → NOT numeric — render as plain text (no animation)
       - "1.4M" → target: 1.4, suffix: "M", decimals: 1
       - "0.5" → target: 0.5, decimals: 1
       - "0.8mm" → target: 0.8, suffix: "mm", decimals: 1
       - "6 days" → target: 6, suffix: " days"
    4. Write a `parseStatValue(value: string)` utility function that returns `{ target: number, prefix: string, suffix: string, decimals: number, isNumeric: boolean }`
       - Use regex: `/^([^\d]*)([\d.]+)(.*)$/` to split into prefix, number, suffix
       - Count decimal places in the number portion for `decimals`
       - If no number found, `isNumeric: false` → render plain text
    5. If `isNumeric` and not reduced motion: render `<CountUp target={parsed.target} suffix={parsed.suffix} prefix={parsed.prefix} decimals={parsed.decimals} className={...} />`
    6. If not numeric OR reduced motion: render static `{value}` as before
    7. Keep ALL existing styling: display-lg text-accent class on the value, caption text on the label
    8. Comment: `// Framer Motion domain — animated number counter (D-01, ANIM-04)`

    **app/page.tsx modification** — Add ScrollReveal to homepage sections (ANIM-09):

    The homepage has multiple sections that should fade-up on scroll-enter. Since app/page.tsx is a Server Component and ScrollReveal is a Client Component, wrap sections individually:

    1. Import `{ ScrollReveal } from "@/components/motion/ScrollReveal"`
    2. Wrap these sections in `<ScrollReveal>`:
       - Capability strip section (section 2) — `<ScrollReveal>` around the SectionHeader + grid
       - Featured case study stat row — `<ScrollReveal delay={0.1}>` around the stat blocks row
       - Portfolio teaser section (section 4) — `<ScrollReveal>` around the SectionHeader + grid
    3. Do NOT wrap Section 1 (hero) — it has its own SplitText animation
    4. Do NOT wrap ManifestoBand or CTABand — they are self-contained components; if they need scroll reveal, it should be added inside them (deferred to keep this plan scoped)
    5. Note: app/page.tsx is a Server Component. Importing a Client Component (ScrollReveal) into a Server Component is valid in Next.js — the Client Component boundary handles itself.

    CRITICAL: StatBlock and PortfolioCard (from Task 1) are now Client Components. They are imported into Server Components (app/page.tsx, HeroDark, case study pages). This is fine — Next.js handles the server/client boundary at the import. But verify the build works.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - StatBlock.tsx line 1 is `"use client"`
    - StatBlock.tsx contains `import.*CountUp.*from.*@/components/motion/CountUp`
    - StatBlock.tsx contains a parseStatValue or similar function with regex
    - StatBlock.tsx contains `useReducedMotion`
    - StatBlock.tsx does NOT import from `gsap` or `@/lib/gsap`
    - app/page.tsx contains `import.*ScrollReveal.*from.*@/components/motion/ScrollReveal`
    - app/page.tsx contains at least 2 `<ScrollReveal` usages
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Stat block numbers animate from 0 to target on scroll-enter (ANIM-04). Homepage sections fade up on scroll (ANIM-09). All in Framer Motion domain (D-01). Reduced motion shows static values immediately.</done>
</task>

</tasks>

<verification>
1. `pnpm build` exits 0 — all routes compile
2. `grep -r "from.*gsap\|from.*@/lib/gsap" components/cards/PortfolioCard.tsx components/ui/StatBlock.tsx app/portfolio/PortfolioGrid.tsx` — NO matches (Framer domain only, D-01)
3. `grep -r "from.*motion/react" components/cards/PortfolioCard.tsx app/portfolio/PortfolioGrid.tsx` — matches in both
4. `grep -r "useReducedMotion" components/cards/PortfolioCard.tsx components/ui/StatBlock.tsx app/portfolio/PortfolioGrid.tsx` — matches in all 3
5. `grep -r "layout" app/portfolio/PortfolioGrid.tsx` — layout prop present for FLIP
</verification>

<success_criteria>
- Portfolio cards hover-lift 4px with shadow (ANIM-05)
- Portfolio filter FLIP animates card reorder (ANIM-06)
- Stat counters animate 0 to target on scroll-enter (ANIM-04)
- Homepage sections fade up on scroll (ANIM-09)
- All animations use Framer Motion only (D-01 boundary)
- All animations respect prefers-reduced-motion
- No GSAP imports in any of these files (D-01)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-webgl/03-03-SUMMARY.md`
</output>
