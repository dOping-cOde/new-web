---
phase: 03-animation-webgl
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - components/three/HeroParticleField.tsx
  - components/three/PointCloudBridge.tsx
  - app/page.tsx
  - content/portfolio/bridgesense.mdx
  - mdx-components.tsx
autonomous: true
requirements: [GL-01, GL-02, GL-03, GL-04, GL-05]
must_haves:
  truths:
    - "Homepage hero displays an indigo particle field drifting behind the headline"
    - "BridgeSense case study displays a rotating point-cloud bridge visualization"
    - "Both scenes load via next/dynamic with ssr:false — no SSR errors"
    - "With prefers-reduced-motion, both scenes show a static SVG fallback instead of canvas"
    - "GPU resources (geometry, material) are explicitly disposed on unmount"
  artifacts:
    - path: "components/three/HeroParticleField.tsx"
      provides: "Homepage particle field scene"
      contains: "Points"
    - path: "components/three/PointCloudBridge.tsx"
      provides: "BridgeSense point-cloud bridge"
      contains: "Points"
    - path: "app/page.tsx"
      provides: "Homepage with lazy-loaded particle field"
      contains: "dynamic"
  key_links:
    - from: "app/page.tsx"
      to: "components/three/HeroParticleField.tsx"
      via: "next/dynamic ssr:false"
      pattern: "dynamic.*HeroParticleField.*ssr.*false"
    - from: "components/three/HeroParticleField.tsx"
      to: "three"
      via: "R3F Canvas + Points geometry"
      pattern: "Canvas|Points"
    - from: "components/three/PointCloudBridge.tsx"
      to: "three"
      via: "R3F Canvas + Points geometry"
      pattern: "Canvas|Points"
---

<objective>
Build two Three.js/R3F scenes: the homepage hero particle field and the BridgeSense point-cloud bridge. Both lazy-loaded with SSR guard, GPU disposal on unmount, and static SVG fallback under reduced motion.

Purpose: These are the only two WebGL moments on the site (per DESIGN.md "ONE moment per page maximum"). They add atmospheric depth to the homepage hero and technical credibility to BridgeSense, the most visually distinctive case study.

Output: 2 new Three.js scene components, wiring into app/page.tsx and BridgeSense MDX
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-animation-webgl/03-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/03-animation-webgl/03-01-SUMMARY.md
@.planning/phases/03-animation-webgl/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 — useReducedMotion hook -->
export function useReducedMotion(): boolean;

<!-- R3F / drei key APIs (from STACK.md) -->
import { Canvas } from "@react-three/fiber";
import { Points, PointMaterial, AdaptiveDpr } from "@react-three/drei";
// Canvas props: frameloop="demand", dpr={[1, 2]}
// Points: drei helper for instanced point rendering (single draw call)

<!-- Current app/page.tsx hero section (lines 76-87) -->
<HeroLight ... showScrollCue={true}>
  {/* TODO: Phase 3 — Three.js particle field */}
  <div className="absolute inset-0 -z-10" />
</HeroLight>

<!-- Current BridgeSense MDX structure (content/portfolio/bridgesense.mdx) -->
<!-- Uses: ProblemSection, SystemSection, Architecture, AIStack, Outcomes -->
<!-- No current Three.js integration point — must add one -->

<!-- Current mdx-components.tsx exports -->
// useMDXComponents returns: ProblemSection, SystemSection, Architecture, AIStack, Outcomes, Quote, TechPill, plus styled HTML elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage hero particle field (HeroParticleField.tsx)</name>
  <files>components/three/HeroParticleField.tsx, app/page.tsx</files>
  <read_first>
    - app/page.tsx (full file — lines 76-87 hero section with children placeholder)
    - .planning/research/STACK.md (WebGL / 3D section, performance rules 1-7)
    - .planning/research/PITFALLS.md (Pitfall 2: GPU disposal; Pitfall 9: SSR window error)
    - lib/useReducedMotion.ts (from Plan 01 — verify export)
    - DESIGN.md (lines 206-207 — "particle/wave field", "performance budget: <60KB JS per scene")
    - .planning/phases/03-animation-webgl/03-CONTEXT.md (D-04: ~2000 indigo particles, low opacity, slow drift, single draw call, <60KB; D-06: next/dynamic ssr:false, frameloop=demand; D-07: explicit GPU disposal)
  </read_first>
  <action>
    **components/three/HeroParticleField.tsx** — Create the particle field scene (GL-01):

    1. `"use client"` directive
    2. This file contains TWO components:
       - `ParticleScene` — the inner R3F scene component (rendered inside Canvas)
       - `HeroParticleField` — the outer wrapper with Canvas, fallback, and disposal

    **ParticleScene inner component:**
    3. Generate ~2000 particle positions using `useMemo`: create a `Float32Array(2000 * 3)` with random positions in a box volume (x: -8 to 8, y: -4 to 4, z: -6 to 2). Seed positions deterministically (not per-render random).
    4. Use drei's `<Points>` component with `<PointMaterial>` for single-draw-call rendering (D-04)
    5. PointMaterial config: `color="#3D2BFF"` (accent indigo), `size={0.03}`, `transparent={true}`, `opacity={0.4}`, `sizeAttenuation={true}`, `depthWrite={false}`
    6. Animate slow drift: use `useFrame((state, delta) => { pointsRef.current.rotation.y += delta * 0.02; pointsRef.current.rotation.x += delta * 0.01; })` — very slow rotation gives drift effect
    7. After creating geometry/material, store refs for disposal

    **HeroParticleField outer wrapper:**
    8. Props: `{ className?: string }`
    9. Import `useReducedMotion` from `@/lib/useReducedMotion`
    10. If reduced motion: render a static SVG fallback instead of Canvas. The SVG should be a subtle noise/dot pattern in indigo at low opacity that suggests the particle field without animation. Use an inline SVG with `<pattern>` of small circles. This satisfies GL-04.
    11. If not reduced motion: render `<Canvas>` with:
        - `frameloop="demand"` (D-06) — BUT override with `"always"` since particles drift continuously. Actually, per D-06 `frameloop="demand"` is for "mobile battery". Use `"always"` for the particle drift, but cap to 30fps on mobile to save battery. Simpler: use `frameloop="always"` with `dpr={[1, 1.5]}` and rely on `<AdaptiveDpr>` for dynamic adjustment.
        - Actually, the simplest correct approach: use `frameloop="always"` since the particles drift. The `useFrame` callback calls `invalidate()` which triggers demand renders. With "demand", you'd need to invalidate every frame for continuous drift — which is the same as "always". Use "always".
        - `camera={{ position: [0, 0, 5], fov: 50 }}`
        - `dpr={[1, 2]}`
        - `style={{ background: 'transparent' }}` — renders on top of the section background
        - `gl={{ alpha: true, antialias: false, powerPreference: 'high-performance' }}`
    12. Include `<AdaptiveDpr pixelated />` inside Canvas for mobile perf
    13. **GPU disposal (GL-05):** Use `useEffect` cleanup in ParticleScene:
        ```
        useEffect(() => {
          return () => {
            if (pointsRef.current) {
              pointsRef.current.geometry?.dispose();
              (pointsRef.current.material as THREE.Material)?.dispose();
            }
          };
        }, []);
        ```
    14. Wrap entire component in `div` with `className` and `aria-hidden="true"` (decorative)
    15. Comment: `// Three.js scene — homepage particle field (GL-01, D-04)`

    **app/page.tsx modification** — Wire in the particle field:
    1. Import via `next/dynamic`: `const HeroParticleField = dynamic(() => import('@/components/three/HeroParticleField'), { ssr: false })` (GL-03, D-06)
    2. Replace the `<div className="absolute inset-0 -z-10" />` placeholder inside `<HeroLight>` children with `<HeroParticleField className="absolute inset-0 -z-10" />`
    3. The dynamic import keeps Three.js out of the initial bundle (GL-03)
    4. Import `dynamic` from `next/dynamic` at top

    **JS budget check (D-04, GL-01):** The scene uses only Points + PointMaterial from drei + basic Three.js. No textures, no post-processing. Combined with tree-shaking, this should stay well under 60KB for the scene-specific code. The Three.js core is shared with BridgeSense.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - components/three/HeroParticleField.tsx contains `"use client"`
    - components/three/HeroParticleField.tsx contains `Points` or `points` (drei Points component or raw)
    - components/three/HeroParticleField.tsx contains `PointMaterial` with `color` including `3D2BFF` (indigo accent)
    - components/three/HeroParticleField.tsx contains `dispose()` calls in cleanup
    - components/three/HeroParticleField.tsx contains `useReducedMotion`
    - components/three/HeroParticleField.tsx contains SVG fallback path for reduced motion
    - components/three/HeroParticleField.tsx contains `aria-hidden`
    - app/page.tsx contains `dynamic.*HeroParticleField.*ssr.*false`
    - app/page.tsx no longer contains the TODO comment about Phase 3 particle field
    - `pnpm build` exits 0 with no "window is not defined" errors
  </acceptance_criteria>
  <done>Homepage hero displays ~2000 indigo particles drifting slowly (GL-01, D-04). Loaded via next/dynamic ssr:false (GL-03, D-06). GPU disposal on unmount (GL-05, D-07). Static SVG fallback under reduced motion (GL-04). Single draw call via Points (D-04). Decorative, aria-hidden.</done>
</task>

<task type="auto">
  <name>Task 2: BridgeSense point-cloud bridge (PointCloudBridge.tsx)</name>
  <files>components/three/PointCloudBridge.tsx, mdx-components.tsx, content/portfolio/bridgesense.mdx</files>
  <read_first>
    - content/portfolio/bridgesense.mdx (full file — need to add a Three.js integration point)
    - mdx-components.tsx (full file — may need a BridgeScene MDX component)
    - .planning/research/STACK.md (WebGL / 3D section, performance rules)
    - .planning/research/PITFALLS.md (Pitfall 2: GPU disposal; Pitfall 9: SSR)
    - .planning/phases/03-animation-webgl/03-CONTEXT.md (D-05: Claude's discretion on procedural vs static, auto-rotate, <60KB; D-06: ssr:false, frameloop=demand; D-07: GPU disposal)
    - DESIGN.md (line 206 — "rotating point-cloud bridge")
  </read_first>
  <action>
    **components/three/PointCloudBridge.tsx** — Create point-cloud bridge scene (GL-02):

    Per D-05 (Claude's discretion), use procedural generation for the bridge shape:

    1. `"use client"` directive
    2. Contains TWO components: `BridgeScene` (inner) and `PointCloudBridge` (outer wrapper)

    **BridgeScene inner component — procedural bridge geometry:**
    3. Generate bridge-shaped point cloud procedurally using `useMemo`:
       - Create ~3000-4000 points that form a recognizable bridge structure
       - Bridge deck: a flat rectangular grid of points (length ~10 units, width ~2 units, y=0)
       - Two arch supports: parametric arch curves on each side, points distributed along parabolic arcs (y = -a*x^2 + h pattern)
       - Pier supports: vertical columns of points at each end and middle
       - Add slight noise (±0.02) to all positions to give a LiDAR-scan quality — imperfect, realistic
       - Total: ~3500 points stored in Float32Array(3500 * 3)
    4. Use `<Points>` + `<PointMaterial>`:
       - `color="#3D2BFF"` (indigo accent, matching brand)
       - `size={0.025}`, `transparent={true}`, `opacity={0.7}`, `sizeAttenuation={true}`, `depthWrite={false}`
    5. Auto-rotate: `useFrame((_, delta) => { groupRef.current.rotation.y += delta * 0.15; })` — slow rotation, one full turn per ~42 seconds
    6. Camera: position [0, 2, 8], looking at origin, fov 45
    7. No mouse interactivity (per D-04 pattern — simple, atmospheric)

    **PointCloudBridge outer wrapper:**
    8. Props: `{ className?: string }`
    9. Import `useReducedMotion` from `@/lib/useReducedMotion`
    10. If reduced motion: render static SVG fallback — a simplified wireframe bridge outline in indigo on dark background. Use inline SVG with line paths forming a bridge silhouette. Satisfies GL-04.
    11. If not reduced motion: render `<Canvas>` with:
        - `frameloop="demand"` (D-06) — this scene auto-rotates, so same consideration as particle field. Use `"always"` for continuous rotation.
        - `dpr={[1, 2]}`
        - `gl={{ alpha: true, antialias: true, powerPreference: 'high-performance' }}`
        - Dark background: `style={{ background: 'transparent' }}` — the parent section provides `bg-bg-dark`
    12. Include `<AdaptiveDpr pixelated />` inside Canvas
    13. **GPU disposal (GL-05):** Same pattern as HeroParticleField — dispose geometry and material in useEffect cleanup
    14. Wrap in `div` with `className`, `role="img"`, `aria-label="Rotating point-cloud visualization of a highway bridge structure"`
    15. Comment: `// Three.js scene — BridgeSense point-cloud bridge (GL-02, D-05)`

    **mdx-components.tsx modification** — Add BridgeVisualization MDX component:
    1. Import `dynamic` from `next/dynamic`
    2. Create a `BridgeVisualization` component (not "use client" — it uses dynamic import):
       ```
       const PointCloudBridge = dynamic(() => import('@/components/three/PointCloudBridge'), { ssr: false });
       function BridgeVisualization({ className = "" }: { children?: React.ReactNode; className?: string }) {
         return (
           <div className={`my-3xl ${className}`} style={{ height: '400px' }}>
             <PointCloudBridge className="w-full h-full" />
           </div>
         );
       }
       ```
    3. Add `BridgeVisualization` to the `useMDXComponents` return object
    4. The dynamic import with `ssr: false` satisfies GL-03

    **content/portfolio/bridgesense.mdx modification** — Insert the visualization:
    1. Add `<BridgeVisualization />` between the `<SystemSection>` and `<Architecture>` sections
    2. This is a natural placement — after describing the system, before the technical architecture
    3. Keep it simple — no props needed, the component handles its own sizing

    **JS budget (D-05, GL-02):** Procedural generation means no external model files. Points + PointMaterial + basic math. Well under 60KB scene-specific code.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - components/three/PointCloudBridge.tsx contains `"use client"`
    - components/three/PointCloudBridge.tsx contains `Points` (drei component)
    - components/three/PointCloudBridge.tsx contains bridge shape generation logic (Float32Array)
    - components/three/PointCloudBridge.tsx contains `dispose()` calls in cleanup
    - components/three/PointCloudBridge.tsx contains `useReducedMotion`
    - components/three/PointCloudBridge.tsx contains SVG fallback for reduced motion
    - components/three/PointCloudBridge.tsx contains `aria-label` with bridge description
    - mdx-components.tsx contains `BridgeVisualization` in the exports
    - mdx-components.tsx contains `dynamic.*PointCloudBridge.*ssr.*false`
    - content/portfolio/bridgesense.mdx contains `<BridgeVisualization`
    - `pnpm build` exits 0 with no SSR errors
  </acceptance_criteria>
  <done>BridgeSense case study displays a rotating point-cloud bridge visualization (GL-02, D-05). Loaded via next/dynamic ssr:false (GL-03, D-06). GPU disposal on unmount (GL-05, D-07). Static SVG fallback under reduced motion (GL-04). Procedural generation, no external models, under 60KB.</done>
</task>

</tasks>

<verification>
1. `pnpm build` exits 0 — no SSR errors, no "window is not defined" (GL-03)
2. `grep -r "ssr.*false" app/page.tsx mdx-components.tsx` — both use dynamic import with ssr:false
3. `grep -r "dispose" components/three/` — both scenes dispose GPU resources (GL-05)
4. `grep -r "useReducedMotion" components/three/` — both scenes check reduced motion (GL-04)
5. `grep -r "aria-hidden\|aria-label" components/three/` — both have accessibility attributes
6. No direct Three.js imports in Server Components
</verification>

<success_criteria>
- Homepage hero has ~2000 indigo particles drifting slowly (GL-01)
- BridgeSense has a rotating point-cloud bridge (GL-02)
- Both loaded via next/dynamic ssr:false (GL-03)
- Both show static SVG fallback under reduced motion (GL-04)
- Both dispose GPU resources on unmount (GL-05)
- Both under 60KB scene JS budget
- Build passes with no SSR errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-webgl/03-04-SUMMARY.md`
</output>
