---
phase: 04-quality
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - README.md
  - app/layout.tsx
  - next.config.mjs
  - components/cards/PortfolioCard.tsx
  - components/sections/HeroDark.tsx
  - components/sections/HeroLight.tsx
  - components/three/HeroParticleFieldLoader.tsx
  - components/three/PointCloudBridgeLoader.tsx
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04, PERF-05, PERF-06, PERF-07, PERF-08, DOC-01, DOC-02, DOC-03]

must_haves:
  truths:
    - "pnpm build produces zero TypeScript errors, zero ESLint errors, zero console warnings"
    - "LCP is under 2.0s on mobile (hero section loads fast)"
    - "CLS is under 0.05 (no layout shifts from fonts, images, or animations)"
    - "Total initial JS on home route is under 250KB"
    - "Three.js scenes are lazy-loaded and each under 60KB"
    - "All images use next/image with proper sizes, AVIF/WebP, blur placeholders where applicable"
    - "Critical fonts preloaded, non-critical deferred"
    - "README.md exists with setup, content-edit, deploy, and placeholder replacement instructions"
    - "Every placeholder asset has a TODO comment"
    - "ContactForm has TODO comment for Resend/Formspree"
  artifacts:
    - path: "README.md"
      provides: "Project documentation with setup, editing, and deployment instructions"
      min_lines: 80
    - path: "app/layout.tsx"
      provides: "Font preloading configuration"
      contains: "preload"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "setup instructions referencing pnpm commands"
      pattern: "pnpm dev|pnpm build"
    - from: "components/three/HeroParticleFieldLoader.tsx"
      to: "next/dynamic"
      via: "ssr: false lazy loading"
      pattern: "ssr.*false"
---

<objective>
Performance optimization for Lighthouse mobile targets (90+/100/100/100) and complete project documentation.

Purpose: Ensure the site meets production performance budgets and a developer can clone, run, edit content, and deploy without additional guidance.
Output: Performance-optimized build, README.md, verified TODO comments on all placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality/04-CONTEXT.md
@.planning/research/PITFALLS.md (Pitfall 6: font loading LCP)

@app/layout.tsx
@lib/fonts.ts
@next.config.mjs
@package.json
@components/three/HeroParticleFieldLoader.tsx
@components/three/PointCloudBridgeLoader.tsx
@components/cards/PortfolioCard.tsx
@components/sections/HeroDark.tsx
@components/sections/HeroLight.tsx
@.env.example

<interfaces>
<!-- Font configuration -->
From lib/fonts.ts:
```typescript
import localFont from 'next/font/local'
// fraunces, inter, jetbrainsMono exported as font objects with .variable property
export const fraunces = localFont({ ... })
export const inter = localFont({ ... })
export const jetbrainsMono = localFont({ ... })
```

<!-- Three.js loaders (already using next/dynamic ssr:false) -->
From components/three/HeroParticleFieldLoader.tsx:
```typescript
// Already uses next/dynamic with ssr: false
```

<!-- Existing image patterns -->
From components/cards/PortfolioCard.tsx:
```typescript
// Placeholder div for missing images, next/image when heroImage is set
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Performance audit and optimization — fonts, images, bundle, build</name>
  <files>app/layout.tsx, lib/fonts.ts, next.config.mjs, components/cards/PortfolioCard.tsx, components/sections/HeroDark.tsx, components/sections/HeroLight.tsx, components/three/HeroParticleFieldLoader.tsx, components/three/PointCloudBridgeLoader.tsx</files>
  <read_first>lib/fonts.ts, app/layout.tsx, next.config.mjs, components/cards/PortfolioCard.tsx, components/sections/HeroDark.tsx, components/sections/HeroLight.tsx, components/three/HeroParticleFieldLoader.tsx, components/three/PointCloudBridgeLoader.tsx</read_first>
  <action>
    **A. Font preloading audit** (per PERF-07, Pitfall 6):
    1. Read lib/fonts.ts — verify font configuration:
       - Inter (body): should use `display: 'swap'` (visible text immediately, swap when loaded)
       - Fraunces (display): should use `display: 'optional'` (prevents FOUT on slow connections — display text can render with fallback)
       - JetBrains Mono (code): should use `display: 'swap'`
    2. Verify `preload: true` is set (or default — Next.js preloads fonts by default)
    3. Verify each font uses variable font files (.woff2 with weight range) — one file per font family
    4. If any font is loading multiple weight files, consolidate to variable font file
    5. Verify Latin subset is specified to reduce file size

    **B. Image optimization audit** (per PERF-06):
    1. Grep all `<Image` and `<img` usages across the project
    2. For each next/image usage, verify:
       - `sizes` prop is set appropriately (not using default which is 100vw)
       - Hero images: `priority={true}` and `loading="eager"` (LCP candidates)
       - Non-hero images: no `priority` (default lazy loading)
       - `fill` images have sized parent containers
    3. For placeholder images (empty heroImage strings in case studies):
       - These use placeholder divs, not broken img tags — verify this pattern
       - When real images are added, they should use `placeholder="blur"` with `blurDataURL`
    4. Check HeroLight and HeroDark for hero image handling:
       - If hero section has a background image, it should have `priority={true}`
       - The Three.js particle field on home hero is the LCP element — ensure the canvas container doesn't cause CLS

    **C. Three.js lazy-loading verification** (per PERF-05):
    1. Verify HeroParticleFieldLoader.tsx uses `next/dynamic` with `{ ssr: false }` — CONFIRMED
    2. Verify PointCloudBridgeLoader.tsx uses `next/dynamic` with `{ ssr: false }` — CONFIRMED
    3. Run `pnpm build` and check the build output for chunk sizes
    4. If any Three.js chunk exceeds 60KB (gzipped), investigate tree-shaking:
       - Ensure imports from `three` are specific (not `import * as THREE from 'three'`)
       - drei imports should be specific components, not full library

    **D. Bundle size check** (per PERF-04):
    1. Run `pnpm build` and inspect the build output
    2. Check the home route's First Load JS — target is under 250KB
    3. If over 250KB, investigate:
       - Are any server-only dependencies leaking to client? (gray-matter, globby, fs)
       - Are Three.js or GSAP being included in non-animated pages?
       - Can any client-side dependencies be deferred?

    **E. CLS prevention** (per PERF-03):
    1. Verify font loading doesn't cause CLS: `next/font` with proper display values handles this
    2. Verify image placeholders don't cause CLS: all images should have explicit dimensions
    3. Verify ScrollTrigger pin-spacer has reserved height (Phase 3 should have handled this)
    4. Check for any dynamically loaded content that shifts layout

    **F. Clean build verification** (per PERF-08):
    1. Run `pnpm build` — must exit 0 with no TypeScript or ESLint errors
    2. Run `pnpm lint` — must pass cleanly
    3. Fix any errors or warnings that appear
    4. Ensure no `console.log` or `console.warn` statements in production code (grep and remove)
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits 0 with no errors
    - pnpm lint passes cleanly
    - Font display values: Inter='swap', Fraunces='optional', JetBrains Mono='swap'
    - Hero images have priority={true}
    - All next/image usages have sizes prop
    - Three.js scenes loaded via next/dynamic with ssr: false
    - Home route First Load JS under 250KB
    - No console.log or console.warn in production code
    - No server-only imports leaking to client bundles
  </acceptance_criteria>
  <done>Build succeeds cleanly, font loading optimized, images properly configured, Three.js lazy-loaded, bundle under 250KB initial route, no console warnings</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md and verify all placeholder TODO comments</name>
  <files>README.md</files>
  <read_first>package.json, .env.example, content/portfolio/idtrm.mdx, components/forms/ContactForm.tsx</read_first>
  <action>
    **A. Create README.md** (per DOC-01):
    Write a comprehensive README at project root with the following sections:

    ```markdown
    # Softwires Technologies — Marketing Website

    Production marketing site for Softwires Technologies, an AI engineering firm.
    Built with Next.js 15, Tailwind CSS v4, GSAP, Three.js, and MDX.

    ## Prerequisites

    - Node.js 20+
    - pnpm 9+

    ## Getting Started

    1. Clone the repository
    2. Install dependencies:
       ```bash
       pnpm install
       ```
    3. Copy the environment file:
       ```bash
       cp .env.example .env.local
       ```
    4. Start the development server:
       ```bash
       pnpm dev
       ```
    5. Open [http://localhost:3000](http://localhost:3000)

    ## Project Structure

    ```
    app/                    # Next.js App Router pages
      layout.tsx            # Root layout (nav, footer, fonts, metadata)
      page.tsx              # Home page
      services/             # Services page
      portfolio/            # Portfolio index + [slug] case studies
      about/                # About page
      contact/              # Contact page
      sitemap.ts            # Auto-generated sitemap
      robots.ts             # robots.txt
    components/
      ui/                   # Design system primitives (Button, Pill, StatBlock, etc.)
      layout/               # Navbar, Footer, Container
      sections/             # Page sections (HeroLight, HeroDark, CTABand, etc.)
      cards/                # Card components (PortfolioCard, ServiceTile, etc.)
      forms/                # ContactForm
      motion/               # Animation wrappers (ScrollReveal, CountUp, etc.)
      three/                # Three.js scenes (particle field, point cloud bridge)
    content/
      portfolio/            # MDX case study files (11 files)
      services.ts           # Capability tile data
    lib/
      fonts.ts              # Self-hosted font definitions
      portfolio.ts          # Case study data loaders
      services.ts           # Service metadata
      types.ts              # TypeScript types and Zod schemas
      jsonld.ts             # JSON-LD structured data generators
      gsap.ts               # GSAP plugin registration
      useReducedMotion.ts   # Accessibility motion preference hook
    public/
      fonts/                # Self-hosted .woff2 font files
      images/               # Static image assets
    DESIGN.md               # Complete design system specification
    ```

    ## Editing Content

    ### Case Studies

    Case studies live in `content/portfolio/` as MDX files. Each file has YAML frontmatter validated by Zod at build time.

    To edit a case study:
    1. Open `content/portfolio/{slug}.mdx`
    2. Update frontmatter fields (title, excerpt, stats, techStack, etc.)
    3. Edit the MDX body below the frontmatter
    4. The build will fail if frontmatter is malformed — check `lib/types.ts` for the schema

    To add a new case study:
    1. Create `content/portfolio/{new-slug}.mdx` following existing file patterns
    2. Add the slug to `relatedSlugs` in other case studies as needed
    3. The sitemap and portfolio index update automatically

    ### Services

    Service data is in `lib/services.ts`. Service copy is in `app/services/page.tsx`.

    ### Home Page

    Featured case studies on the home page are hardcoded in `app/page.tsx` (`FEATURED_CARDS` array).

    ## Replacing Placeholder Assets

    This site ships with placeholder images and TODO comments marking everything that needs real assets before launch.

    Search for all placeholders:
    ```bash
    grep -rn "TODO" --include="*.tsx" --include="*.ts" --include="*.mdx" .
    ```

    Key placeholders:
    - **Hero images**: Case study hero images referenced in MDX frontmatter (`heroImage` field)
    - **Portfolio card images**: Same hero images shown as thumbnails
    - **Service diagrams**: Placeholder boxes in service sections on /services
    - **Partner logos**: About page partnership section
    - **Client names**: Some case studies use bracketed placeholder names
    - **Client quotes**: Pullquote components have placeholder text

    ## Contact Form

    The contact form currently constructs a `mailto:` link. To wire server-side submission:
    1. See the TODO in `components/forms/ContactForm.tsx` (line ~118)
    2. Options: Resend (recommended) or Formspree
    3. Create an API route at `app/api/contact/route.ts`

    ## Deployment

    The site is configured for Vercel deployment (standard Next.js, not static export).

    1. Push to GitHub
    2. Import the repository in Vercel
    3. Set the environment variable:
       - `NEXT_PUBLIC_SITE_URL` = your production domain (e.g., `https://softwires.in`)
    4. Deploy

    The site uses `pnpm` — Vercel auto-detects this from `pnpm-lock.yaml`.

    ## Scripts

    | Command | Description |
    |---------|-------------|
    | `pnpm dev` | Start development server |
    | `pnpm build` | Production build |
    | `pnpm start` | Start production server |
    | `pnpm lint` | Run ESLint |

    ## Tech Stack

    | Technology | Purpose |
    |-----------|---------|
    | Next.js 15 | App Router, SSG, image optimization |
    | Tailwind CSS v4 | Utility-first styling (CSS-first config) |
    | GSAP 3.15 | Scroll animations, text reveals |
    | Motion (Framer) | Micro-interactions, hover effects |
    | Three.js + R3F | WebGL particle field, point-cloud bridge |
    | MDX | Case study content |
    | Zod | Frontmatter validation |

    ## Design System

    See `DESIGN.md` for the complete design system specification including colors, typography, spacing, and component patterns.
    ```

    **B. Verify TODO comments on all placeholder assets** (per DOC-02):
    1. Grep for all TODO comments in the codebase
    2. Verify the following have TODO comments:
       - Placeholder hero images in case study MDX files
       - Placeholder diagram boxes on services page
       - Placeholder partner logos on about page
       - Placeholder client names in case study MDX frontmatter
       - Placeholder client quotes in Pullquote component
       - Service page diagram placeholders
       - Home page featured case study image placeholder
    3. If any placeholder asset is missing a TODO comment, add one

    **C. Verify ContactForm TODO** (per DOC-03):
    - `components/forms/ContactForm.tsx` line ~118 already has: `// TODO: replace with Resend or Formspree integration — see DOC-03`
    - CONFIRMED present. No changes needed.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && test -f README.md && grep -q "pnpm dev" README.md && grep -q "pnpm build" README.md && grep -q "Vercel" README.md && grep -q "NEXT_PUBLIC_SITE_URL" README.md && grep -q "TODO.*Resend\|TODO.*Formspree" components/forms/ContactForm.tsx && echo "Documentation complete"</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at project root
    - README includes: prerequisites (Node 20+, pnpm 9+)
    - README includes: getting started (install, env, dev server)
    - README includes: project structure overview
    - README includes: how to edit case studies (MDX files, frontmatter, Zod schema)
    - README includes: how to replace placeholder assets (grep command for TODOs)
    - README includes: how to deploy to Vercel (env var, auto-detect pnpm)
    - README includes: contact form TODO for Resend/Formspree
    - All placeholder assets have TODO comments
    - ContactForm has TODO for Resend/Formspree at line ~118
  </acceptance_criteria>
  <done>README.md covers setup, content editing, placeholder replacement, deployment. All placeholder assets have TODO comments. ContactForm TODO for Resend/Formspree confirmed.</done>
</task>

</tasks>

<verification>
- README.md exists and covers all required sections
- `pnpm build` exits 0
- `pnpm lint` passes
- grep -rn "TODO" finds placeholder markers on all known placeholder assets
- ContactForm.tsx has Resend/Formspree TODO
- Build output shows home route First Load JS under 250KB
</verification>

<success_criteria>
- All 11 requirements (PERF-01 through PERF-08, DOC-01, DOC-02, DOC-03) satisfied
- Clean build with zero errors/warnings
- Font loading optimized (swap/optional display strategies)
- Images use next/image with sizes, priority on heroes only
- Three.js scenes lazy-loaded, under 60KB each
- Home route initial JS under 250KB
- README.md is comprehensive and actionable
- Every placeholder has a TODO comment
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality/04-04-SUMMARY.md`
</output>
