---
phase: 04-quality
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified: []
autonomous: false
requirements: [PERF-01]

must_haves:
  truths:
    - "pnpm build exits 0 with no errors"
    - "All 32 Phase 4 requirements have been addressed in Plans 01-04"
    - "Human has visually verified the site and confirmed readiness for handoff"
  artifacts: []
  key_links: []
---

<objective>
Final build verification and human checkpoint to confirm the site is production-ready.

Purpose: Validate that all SEO, accessibility, performance, and documentation requirements are met before the project is marked complete.
Output: Verification results and human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality/04-CONTEXT.md
@.planning/phases/04-quality/04-01-SUMMARY.md
@.planning/phases/04-quality/04-02-SUMMARY.md
@.planning/phases/04-quality/04-03-SUMMARY.md
@.planning/phases/04-quality/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 4 requirements</name>
  <files></files>
  <read_first>app/layout.tsx, app/page.tsx, app/sitemap.ts, app/robots.ts, lib/jsonld.ts, README.md, components/forms/ContactForm.tsx</read_first>
  <action>
    Run a comprehensive automated check against all 32 requirements. Report PASS/FAIL for each.

    **SEO checks:**
    ```bash
    # SEO-01: Unique titles and descriptions on every route
    for f in app/page.tsx app/services/page.tsx app/portfolio/page.tsx app/about/page.tsx app/contact/page.tsx; do
      grep -q "title:" "$f" && echo "PASS: $f has title" || echo "FAIL: $f missing title"
      grep -q "description:" "$f" && echo "PASS: $f has description" || echo "FAIL: $f missing description"
    done

    # SEO-02: OG + Twitter tags
    grep -q "openGraph" app/layout.tsx && echo "PASS: SEO-02 root OG"
    grep -q "twitter" app/layout.tsx && echo "PASS: SEO-02 root Twitter"

    # SEO-03: Dynamic OG images
    test -f app/opengraph-image.tsx && echo "PASS: SEO-03 default OG image"
    test -f "app/portfolio/[slug]/opengraph-image.tsx" && echo "PASS: SEO-03 per-case-study OG"

    # SEO-04: Organization JSON-LD
    grep -q "Organization" lib/jsonld.ts && echo "PASS: SEO-04"

    # SEO-05: WebSite JSON-LD
    grep -q "WebSite" lib/jsonld.ts && echo "PASS: SEO-05"

    # SEO-06: Service JSON-LD
    grep -q "Service" lib/jsonld.ts && echo "PASS: SEO-06"

    # SEO-07: Article + BreadcrumbList JSON-LD
    grep -q "Article" lib/jsonld.ts && grep -q "BreadcrumbList" lib/jsonld.ts && echo "PASS: SEO-07"

    # SEO-08: sitemap.ts
    test -f app/sitemap.ts && echo "PASS: SEO-08"

    # SEO-09: robots.ts
    test -f app/robots.ts && echo "PASS: SEO-09"

    # SEO-10: Canonical URLs via metadataBase
    grep -q "metadataBase" app/layout.tsx && echo "PASS: SEO-10"

    # SEO-11: Semantic HTML (spot check)
    grep -q '<main' app/layout.tsx && echo "PASS: SEO-11 main element"

    # SEO-12: Image alt text
    grep -rq "alt=" components/sections/HeroDark.tsx && echo "PASS: SEO-12 hero alt"
    ```

    **Accessibility checks:**
    ```bash
    # A11Y-01: WCAG AA + AAA body text
    grep -q 'lang="en"' app/layout.tsx && echo "PASS: A11Y-01 lang"

    # A11Y-02: Keyboard navigation (code-level check)
    grep -q "aria-expanded" components/layout/Navbar.tsx && echo "PASS: A11Y-02 nav keyboard"

    # A11Y-03: Focus indicators
    grep -q "focus-visible" app/globals.css && echo "PASS: A11Y-03"

    # A11Y-04: Skip to content
    grep -q "skip-to-content" app/layout.tsx && echo "PASS: A11Y-04"

    # A11Y-05: Reduced motion
    grep -rq "useReducedMotion" components/ && echo "PASS: A11Y-05"

    # A11Y-06: Form aria-live
    grep -q "aria-live" components/forms/ContactForm.tsx && echo "PASS: A11Y-06"

    # A11Y-07: Accessible names
    grep -q "aria-label" components/layout/Navbar.tsx && echo "PASS: A11Y-07"

    # A11Y-08: Color independence (manual verification needed)
    echo "A11Y-08: Requires manual verification"

    # A11Y-09: Heading order (manual verification needed)
    echo "A11Y-09: Requires manual verification"
    ```

    **Performance checks:**
    ```bash
    # PERF-08: Clean build
    pnpm build 2>&1 | tail -5

    # PERF-04: Bundle size (from build output)
    # Check "First Load JS" for home route in build output

    # PERF-05: Three.js lazy-loaded
    grep -rq "ssr.*false" components/three/ && echo "PASS: PERF-05"

    # PERF-07: Font display values
    grep -q "display" lib/fonts.ts && echo "PASS: PERF-07"
    ```

    **Documentation checks:**
    ```bash
    # DOC-01: README exists
    test -f README.md && echo "PASS: DOC-01"

    # DOC-02: Placeholder TODOs
    grep -rn "TODO" --include="*.tsx" --include="*.ts" --include="*.mdx" . | wc -l
    echo "(should be 15+ TODO comments across placeholder assets)"

    # DOC-03: ContactForm TODO
    grep -q "TODO.*Resend\|TODO.*Formspree" components/forms/ContactForm.tsx && echo "PASS: DOC-03"
    ```

    Compile results into a verification table in the summary.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | grep -q "Generating static pages" && echo "Build successful"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits 0
    - All automated checks pass (SEO-01 through SEO-12, A11Y-01 through A11Y-09 where automatable, PERF-05/07/08, DOC-01/02/03)
    - Build output shows home route First Load JS under 250KB
    - Verification table compiled with PASS/FAIL for each requirement
  </acceptance_criteria>
  <done>All 32 requirements verified via automated checks where possible, build succeeds, verification table compiled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of production readiness</name>
  <files></files>
  <action>
    Present the human with verification instructions for visual and interactive testing of all Phase 4 work.
  </action>
  <what-built>Complete Phase 4 quality pass: SEO metadata + JSON-LD + OG images + sitemap + robots.txt + accessibility audit/fixes + performance optimization + README documentation</what-built>
  <how-to-verify>
    1. **Build check**: Run `pnpm build` — should exit 0 with no errors
    2. **Start the dev server**: `pnpm dev` and open http://localhost:3000

    3. **SEO verification**:
       - Visit http://localhost:3000/sitemap.xml — should list 16+ URLs
       - Visit http://localhost:3000/robots.txt — should allow all, reference sitemap
       - View page source on home page — look for JSON-LD scripts (Organization + WebSite)
       - View page source on /services — look for 6 Service JSON-LD blocks
       - View page source on any case study — look for Article + BreadcrumbList JSON-LD
       - Check that every page has a unique title and meta description in source

    4. **Accessibility verification**:
       - Press Tab through the entire home page — focus rings should be visible on every interactive element
       - On first Tab press, skip-to-content link should appear
       - Open mobile nav (hamburger) — Tab should cycle through nav links only (focus trap)
       - Press Escape — mobile nav should close
       - On /contact, submit empty form — error messages should appear and be announced

    5. **Performance verification** (optional but recommended):
       - Run Lighthouse in Chrome DevTools on home page (mobile simulation)
       - Target: 90+ Performance, 100 Accessibility, 100 Best Practices, 100 SEO
       - Check LCP (should be under 2.0s), CLS (under 0.05)

    6. **Documentation verification**:
       - Open README.md — verify it covers setup, content editing, deployment
       - Run `grep -rn "TODO" --include="*.tsx" --include="*.ts" --include="*.mdx" .` — verify placeholder TODOs exist

    7. **prefers-reduced-motion** (if macOS):
       - System Settings > Accessibility > Display > Reduce Motion = ON
       - Refresh home page — particle field should show static fallback, no animations
  </how-to-verify>
  <verify>Human approves the site as production-ready</verify>
  <done>Human has verified SEO, accessibility, performance, and documentation — Phase 4 is complete</done>
  <resume-signal>Type "approved" to mark Phase 4 complete, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
- pnpm build succeeds
- All 32 requirements verified
- Human sign-off received
</verification>

<success_criteria>
- Build passes with zero errors
- Human confirms: SEO metadata visible in source, keyboard navigation works, focus rings visible, Lighthouse scores meet targets, README is helpful
- Phase 4 marked complete — site is production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality/04-05-SUMMARY.md`
</output>
