---
phase: 04-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/services/page.tsx
  - app/services/AnchorNav.tsx
  - app/portfolio/page.tsx
  - app/portfolio/PortfolioGrid.tsx
  - app/portfolio/[slug]/page.tsx
  - app/about/page.tsx
  - app/contact/page.tsx
  - app/not-found.tsx
  - components/layout/Navbar.tsx
  - components/layout/Footer.tsx
  - components/forms/ContactForm.tsx
  - components/ui/Button.tsx
  - components/cards/PortfolioCard.tsx
  - components/cards/ServiceTile.tsx
  - components/cards/CapabilityCard.tsx
  - components/sections/HeroLight.tsx
  - components/sections/HeroDark.tsx
  - components/sections/CTABand.tsx
autonomous: true
requirements: [A11Y-01, A11Y-02, A11Y-03, A11Y-04, A11Y-05, A11Y-06, A11Y-07, A11Y-08, A11Y-09]

must_haves:
  truths:
    - "Entire site is navigable using only keyboard — Tab, Enter, Escape, arrow keys work for all interactive elements"
    - "Focus rings are always visible: 2px indigo outline, 2px offset on all focusable elements"
    - "Skip-to-content link works and is the first focusable element"
    - "prefers-reduced-motion disables all GSAP, Framer Motion, and Three.js animations"
    - "Form errors are announced via aria-live='polite'"
    - "All interactive elements have accessible names (buttons, links, inputs, selects)"
    - "Color is never the sole carrier of information"
    - "Heading order is strictly h1 -> h2 -> h3 on every page, no skipped levels"
    - "Body text meets AAA contrast (7:1 ratio minimum)"
  artifacts:
    - path: "app/globals.css"
      provides: "Global focus ring styles, skip-to-content styles"
      contains: "focus-visible"
    - path: "components/layout/Navbar.tsx"
      provides: "Keyboard-navigable nav with ARIA labels"
      contains: "aria-label"
  key_links:
    - from: "app/globals.css"
      to: "all interactive elements"
      via: "focus-visible global styles"
      pattern: "focus-visible"
    - from: "components/forms/ContactForm.tsx"
      to: "error display"
      via: "aria-live='polite'"
      pattern: "aria-live"
---

<objective>
WCAG 2.1 AA accessibility audit and fixes across the entire site. Covers keyboard navigation, focus management, ARIA labels, heading hierarchy, color contrast, and reduced motion compliance.

Purpose: Achieve Lighthouse Accessibility score of 100 and full WCAG 2.1 AA compliance (AAA for body text contrast).
Output: Accessibility fixes across all component and page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality/04-CONTEXT.md

@app/layout.tsx
@app/globals.css
@components/ui/Button.tsx
@components/layout/Navbar.tsx
@components/layout/Footer.tsx
@components/forms/ContactForm.tsx
@components/sections/HeroLight.tsx
@components/sections/HeroDark.tsx
@components/cards/PortfolioCard.tsx

<interfaces>
<!-- Existing a11y features to verify and extend -->

From app/layout.tsx:
```typescript
<a href="#main-content" className="skip-to-content">Skip to content</a>
<main id="main-content">{children}</main>
```

From app/globals.css:
```css
.skip-to-content { /* Already implemented */ }
.skip-to-content:focus { /* Already implemented */ }
```

From components/ui/Button.tsx:
```typescript
"focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2"
```

From components/forms/ContactForm.tsx:
```typescript
aria-live="polite" // Already on error announcements
```

From lib/useReducedMotion.ts:
```typescript
export function useReducedMotion(): boolean
// Custom hook — framework-agnostic, used by GSAP, Framer Motion, and Three.js
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keyboard navigation and focus management audit</name>
  <files>app/globals.css, components/layout/Navbar.tsx, components/layout/Footer.tsx, components/ui/Button.tsx, components/cards/PortfolioCard.tsx, components/cards/ServiceTile.tsx, components/cards/CapabilityCard.tsx, app/services/AnchorNav.tsx, app/portfolio/PortfolioGrid.tsx</files>
  <read_first>app/globals.css, components/layout/Navbar.tsx, components/layout/Footer.tsx, components/ui/Button.tsx, components/cards/PortfolioCard.tsx, components/cards/ServiceTile.tsx, components/cards/CapabilityCard.tsx, app/services/AnchorNav.tsx, app/portfolio/PortfolioGrid.tsx</read_first>
  <action>
    Systematic keyboard navigation and focus indicator audit. For each interactive component:

    **A. Focus ring consistency** (per A11Y-03, DESIGN.md spec: 2px indigo, 2px offset):
    1. Check `app/globals.css` for a global focus-visible rule. If not present, add:
       ```css
       /* Global focus ring — WCAG 2.1 AA (A11Y-03) */
       :focus-visible {
         outline: 2px solid var(--color-accent);
         outline-offset: 2px;
       }
       ```
       This catches any interactive element that doesn't have component-level focus styles.
    2. Verify Button.tsx already has `focus-visible:outline-2 focus-visible:outline-accent focus-visible:outline-offset-2` — CONFIRMED.
    3. Check EVERY card component (PortfolioCard, ServiceTile, CapabilityCard) — if they use `<a>` or `<Link>`, the global rule covers them. If they use `<div>` with onClick, they need `tabIndex={0}` and `onKeyDown` handler.
    4. Ensure no component uses `outline-none` or `outline-hidden` without providing a replacement focus indicator.

    **B. Navbar keyboard navigation** (per A11Y-02):
    1. Read Navbar.tsx — verify all nav links are keyboard-accessible
    2. Mobile hamburger button: must have `aria-label="Open menu"` / `aria-label="Close menu"` (toggled), `aria-expanded` attribute
    3. Mobile overlay: must trap focus when open (Tab cycles through overlay links only, not background), Escape key closes overlay
    4. If focus trap is missing, add it: on overlay open, focus the first link; on Tab from last link, wrap to first; on Escape, close and return focus to hamburger button

    **C. Footer keyboard navigation** (per A11Y-02):
    1. Verify all footer links are standard `<a>` tags (keyboard-accessible by default)
    2. Add `aria-label="Footer navigation"` to the footer `<nav>` if not present

    **D. Portfolio filter bar** (per A11Y-02):
    1. Read PortfolioGrid.tsx — verify filter buttons are `<button>` elements (not divs)
    2. If buttons are divs, change to `<button>` with `aria-pressed` to indicate active filter
    3. Ensure active filter state is communicated (not just visual color) — add `aria-pressed="true"` on active filter button

    **E. Anchor nav on services page** (per A11Y-02):
    1. Verify AnchorNav.tsx links are `<a>` tags with valid hrefs
    2. Add `aria-label="Service navigation"` to the nav container
    3. Active state must have `aria-current="true"` (not just visual)

    **F. Card components** (per A11Y-02, A11Y-07):
    1. PortfolioCard: entire card should be a link. If card wraps a Link, verify the link has meaningful text (not just "Read more")
    2. ServiceTile: if clickable, ensure accessible name
    3. CapabilityCard: if clickable, ensure accessible name

    **G. Verify no interactive element lacks accessible name** (per A11Y-07):
    - Grep for icon-only buttons (buttons with only SVG/icon children) — add aria-label
    - Grep for links with only icon children — add aria-label
    - Any `<button>` or `<a>` without text content needs `aria-label`
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && grep -q "focus-visible" app/globals.css && grep -q "aria-label" components/layout/Navbar.tsx && grep -q "aria-label\|aria-pressed" app/portfolio/PortfolioGrid.tsx && echo "A11y keyboard audit complete"</automated>
  </verify>
  <acceptance_criteria>
    - Global focus-visible rule in globals.css (2px accent outline, 2px offset)
    - No outline-none or outline-hidden without replacement focus indicator
    - Navbar hamburger has aria-label and aria-expanded
    - Mobile nav overlay traps focus and closes on Escape
    - Portfolio filter buttons have aria-pressed for active state
    - Services anchor nav has aria-label and aria-current on active link
    - All icon-only buttons have aria-label
    - All card links have meaningful accessible names
  </acceptance_criteria>
  <done>Every interactive element is keyboard-accessible with visible focus rings, ARIA labels, and proper state communication</done>
</task>

<task type="auto">
  <name>Task 2: ARIA attributes, contrast verification, color independence, heading order, and reduced motion completeness</name>
  <files>components/forms/ContactForm.tsx, components/sections/HeroLight.tsx, components/sections/HeroDark.tsx, components/sections/CTABand.tsx, app/layout.tsx, app/page.tsx, app/services/page.tsx, app/about/page.tsx, app/contact/page.tsx, app/not-found.tsx, app/portfolio/[slug]/page.tsx</files>
  <read_first>components/forms/ContactForm.tsx, components/sections/HeroLight.tsx, components/sections/HeroDark.tsx, components/sections/CTABand.tsx, app/about/page.tsx, app/contact/page.tsx, app/not-found.tsx, app/page.tsx</read_first>
  <action>
    **A. Form accessibility** (per A11Y-06):
    1. ContactForm.tsx already has aria-live="polite" — CONFIRMED
    2. Verify every `<input>` and `<select>` and `<textarea>` has:
       - An associated `<label>` with `htmlFor` matching the input `id`
       - `aria-required="true"` on required fields
       - `aria-invalid="true"` when validation fails
       - `aria-describedby` pointing to the error message element when error is shown
    3. If error messages are shown inline, ensure each error has a unique id that the input's `aria-describedby` references
    4. Submit button must have clear text ("Send message" not just "Submit")

    **B. Skip-to-content verification** (per A11Y-04):
    1. Verify app/layout.tsx has `<a href="#main-content" className="skip-to-content">Skip to content</a>` as the FIRST focusable element in body — CONFIRMED
    2. Verify `<main id="main-content">` exists — CONFIRMED
    3. Verify skip-to-content is visually hidden until focused (CSS in globals.css) — CONFIRMED
    4. No changes needed if all present.

    **C. prefers-reduced-motion completeness** (per A11Y-05):
    1. Verify useReducedMotion hook exists and is used in: GSAP components, Three.js loaders, Framer Motion components
    2. This was thoroughly implemented in Phase 3 — verify it's still intact, no regression
    3. Grep for `useReducedMotion` across all animated components to confirm coverage

    **D. Color independence** (per A11Y-08):
    1. Review all instances where color communicates state:
       - Active filter button (portfolio): must have underline/bold OR aria-pressed, not just color change
       - Active anchor nav link (services): must have underline/weight OR aria-current, not just color
       - Form validation errors: must have error text message, not just red border
       - Link hover states: underline or other non-color indicator
    2. Fix any instance where color is the SOLE carrier of information

    **E. Body text contrast AAA** (per A11Y-01):
    1. Design system uses:
       - Body text: `text-text` on `bg-bg-light` — verify this is 7:1+ ratio (AAA)
       - Muted text: `text-text-muted` on `bg-bg-light` — verify at least 4.5:1 (AA)
       - Inverted: `text-text-inverted` on `bg-bg-dark` — verify 7:1+ (AAA for body)
       - Muted dark: `text-text-muted-dark` on `bg-bg-dark` — verify at least 4.5:1 (AA)
    2. Check CSS variable values in globals.css for actual hex/oklch values
    3. If any body text fails AAA (7:1), adjust the color to meet the ratio
    4. Muted text and captions only need AA (4.5:1)

    **F. Heading order** (per A11Y-09):
    Systematically verify every page:
    - Home (app/page.tsx): h1 (HeroLight) -> h2 (capability strip, featured case study, portfolio teaser, manifesto, CTA)
    - Services (app/services/page.tsx): h1 (HeroLight) -> h2 (each service section) -> h3 (engagement model)
    - Portfolio (app/portfolio/page.tsx): h1 (HeroLight) -> h2 (filter section header if any)
    - About (app/about/page.tsx): h1 (HeroLight) -> h2 (origin, principles, sectors) -> h3 (if any)
    - Contact (app/contact/page.tsx): h1 (HeroLight) -> h2 (form section) -> h3 (if any)
    - Case studies: h1 (HeroDark) -> h2 (sections) -> h3 (subsections)
    - 404 (app/not-found.tsx): h1 (404 title)

    Fix any heading that skips a level (e.g., h1 -> h3 must become h1 -> h2 -> h3, or the h3 becomes h2).

    **G. HTML lang attribute** (per A11Y-01):
    - Verify `<html lang="en">` in layout.tsx — CONFIRMED
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && grep -q "aria-required\|aria-invalid\|aria-describedby" components/forms/ContactForm.tsx && grep -q "useReducedMotion" components/three/HeroParticleFieldLoader.tsx 2>/dev/null || grep -rq "useReducedMotion" components/three/ && grep -q 'lang="en"' app/layout.tsx && echo "A11y ARIA + contrast audit complete"</automated>
  </verify>
  <acceptance_criteria>
    - ContactForm inputs have aria-required, aria-invalid, aria-describedby for error messages
    - Skip-to-content is first focusable element, targets #main-content
    - useReducedMotion used across all animated components (GSAP, Framer, Three.js)
    - Color is never sole carrier of information (active states have non-color indicators)
    - Body text on light background meets 7:1 contrast (AAA)
    - Body text on dark background meets 7:1 contrast (AAA)
    - Muted text meets 4.5:1 contrast (AA minimum)
    - html has lang="en"
    - Every page has exactly one h1, heading levels never skip
  </acceptance_criteria>
  <done>Full WCAG 2.1 AA compliance (AAA for body text), all ARIA attributes in place, color independence verified, heading hierarchy enforced across all pages</done>
</task>

</tasks>

<verification>
- Run `pnpm build` to verify no TypeScript errors from added ARIA attributes
- Tab through every page in browser to verify keyboard navigation
- Enable "Reduce Motion" in OS settings and verify no animations play
- Screen reader test: focus on form, trigger error, verify error is announced
- Check heading order with browser dev tools accessibility tree
</verification>

<success_criteria>
- All 9 accessibility requirements (A11Y-01 through A11Y-09) satisfied
- Lighthouse Accessibility score: 100
- Full keyboard navigation works on every page
- Focus rings visible on all interactive elements (2px indigo, 2px offset)
- Form errors announced via aria-live
- Body text meets AAA contrast (7:1)
- No heading level skipped anywhere
- prefers-reduced-motion respected by all animation systems
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality/04-03-SUMMARY.md`
</output>
