---
phase: 02-content-pages
plan: 06
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - app/portfolio/page.tsx
autonomous: true
requirements: [PAGE-03]
must_haves:
  truths:
    - "Portfolio index renders hero, filter bar, and 11 portfolio cards in a 3-column grid"
    - "Filter bar has pills for All, Energy, Healthcare, Infrastructure, Insurance, Data & Analytics"
    - "Clicking a filter pill shows only matching cards (client-side filtering)"
    - "Cards appear in the exact order specified in the build spec"
  artifacts:
    - path: "app/portfolio/page.tsx"
      provides: "Complete Portfolio index page with filter and card grid"
      contains: "Eleven systems"
  key_links:
    - from: "app/portfolio/page.tsx"
      to: "lib/portfolio.ts"
      via: "imports getAllCaseStudies for card data"
      pattern: "import.*getAllCaseStudies"
    - from: "app/portfolio/page.tsx"
      to: "components/cards/PortfolioCard.tsx"
      via: "renders PortfolioCard for each case study"
      pattern: "PortfolioCard"
---

<objective>
Build the Portfolio index page with client-side category filtering.

Purpose: The Portfolio page shows all 11 projects with filter-by-category capability. Technical buyers can browse by industry (Energy, Healthcare, Infrastructure, Insurance, Data & Analytics) or view all.
Output: Fully functional /portfolio page with filter bar and responsive card grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-pages/02-CONTEXT.md

Build spec for Portfolio page:
@/Users/george/Downloads/softwires_claude_code_prompt.md (section 4.3 — Portfolio page brief)

Prior plan SUMMARYs:
@.planning/phases/02-content-pages/02-01-SUMMARY.md
@.planning/phases/02-content-pages/02-02-SUMMARY.md
@.planning/phases/02-content-pages/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Portfolio index page with filter bar and card grid</name>
  <files>app/portfolio/page.tsx</files>
  <read_first>
    - /Users/george/Downloads/softwires_claude_code_prompt.md (section 4.3 — Portfolio page brief)
    - /Users/george/DEV/softwire/DESIGN.md
    - /Users/george/DEV/softwire/lib/portfolio.ts
    - /Users/george/DEV/softwire/lib/types.ts
    - /Users/george/DEV/softwire/components/cards/PortfolioCard.tsx
    - /Users/george/DEV/softwire/components/sections/HeroLight.tsx
    - /Users/george/DEV/softwire/components/sections/CTABand.tsx
    - /Users/george/DEV/softwire/components/ui/Pill.tsx
    - /Users/george/DEV/softwire/components/layout/Container.tsx
  </read_first>
  <action>
Create `app/portfolio/` directory (if not exists) and `page.tsx`.

This page has TWO parts:
1. A Server Component wrapper that loads data and renders hero/CTA
2. A "use client" component for the filter bar + card grid (needs useState for active filter)

**Page structure:**

Export page metadata:
```typescript
export const metadata: Metadata = {
  title: "Portfolio",
  description: "Eleven AI systems built for production — from transformer monitoring to cancer screening. Browse by industry: Energy, Healthcare, Infrastructure, Insurance, Data & Analytics.",
};
```

**Server Component (page.tsx):**
- Import `getAllCaseStudies` from `@/lib/portfolio`
- Call `const caseStudies = await getAllCaseStudies()` at the top of the async component
- Render:
  1. `<HeroLight kicker="PORTFOLIO" headline="Eleven systems. Built for production." headlineSize="text-display-lg" intro="Real AI systems deployed in energy grids, hospitals, bridges, and insurance platforms. Each one runs in production." />`
  2. `<PortfolioGrid caseStudies={caseStudies} />` — the client component
  3. `<CTABand />`

**Client Component (PortfolioGrid — can be inline in same file or separate):**
Mark as "use client".

Props:
```typescript
interface PortfolioGridProps {
  caseStudies: CaseStudyFrontmatter[];
}
```

State: `const [activeFilter, setActiveFilter] = useState<string>("All")`

Filter bar (sticky below navbar at `top: 64px`, `z-40`):
- Background: `bg-bg-light/95 backdrop-blur-sm`, border `border-b border-border-light`
- Container wrapper
- Row of `<Pill>` components for each category from CATEGORIES (imported from lib/types.ts):
  - "All", "Energy", "Healthcare", "Infrastructure", "Insurance", "Data & Analytics"
  - Each Pill: `active={activeFilter === category}`, `onClick={() => setActiveFilter(category)}`
  - Flex row with `gap-sm`, `py-md` padding for the filter bar
  - On mobile: horizontal scroll `overflow-x-auto`, no wrapping

Filtering logic:
```typescript
const filtered = activeFilter === "All"
  ? caseStudies
  : caseStudies.filter(cs => cs.category === activeFilter);
```

Card grid:
- `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-xl mt-2xl` inside a `<Container>`
- Map `filtered` to `<PortfolioCard>` components
- Each card receives: slug, title, category, subcategory, excerpt, heroImage, heroImageAlt from the case study frontmatter
- Cards MUST appear in this exact order (when "All" is selected): iDTRM, BridgeSense, Salt-Lick, AI-Copter, FWA Platform, Unified Semantic Fabric, BI Acceleration Engine, Multidimensional OLAP Modernization, Cloud Analytics Cost Optimization, Conversational Data Agent, Enterprise Reporting Suite
- The sort order should be maintained by getAllCaseStudies (Plan 01 handles this)
- FLIP animation for filter transitions will be added in Phase 3 — for now, cards just appear/disappear instantly on filter change
- Add a `key={cs.slug}` on each card for React reconciliation

Empty state: if `filtered.length === 0` (shouldn't happen with current data), show `<p className="text-body text-text-muted text-center py-2xl">No projects in this category.</p>`

NOTE: Since the MDX content files don't exist yet (they'll be created in Plans 07-08), getAllCaseStudies() will initially return an empty array or throw. The executor should handle this gracefully — either create stub MDX files with minimal frontmatter, or hardcode the case study data temporarily. The preferred approach: create 11 minimal stub MDX files in content/portfolio/ with just frontmatter (no body content), so getAllCaseStudies() works. Each stub should have YAML frontmatter (delimited by triple-dash lines) with these fields:

title, slug, tier, category, subcategory, client, year, heroImage, heroImageAlt, excerpt, stats (array of value/label objects), techStack (string array), relatedSlugs (string array).

Example for iDTRM: title="iDTRM", slug="idtrm", tier="A", category="Energy", subcategory="IoT", client="[A leading central-Indian DISCOM]", year=2024, heroImage="/images/portfolio/idtrm-hero.jpg", heroImageAlt="iDTRM dashboard overlay on a substation photograph", excerpt="Real-time monitoring across 8,000 distribution transformers.", stats=[{value:"8,000+", label:"Transformers monitored"}, {value:"5 min", label:"Refresh interval"}, {value:"40 days", label:"Load survey retention"}, {value:"0.5", label:"Accuracy class"}], techStack=["GPRS","HTTP","RS485 Modbus","AWS","Time-series DB","ML"], relatedSlugs=["bridgesense","ai-copter"]. MDX body should just have a placeholder comment.

Create ALL 11 stubs with correct frontmatter for each project. Use the data from section 4.3 and section 4.4 of the build spec. This enables the Portfolio page AND the case study detail pages to have data to work with.

Stub frontmatter for all 11 (slug / category / subcategory / client):
1. idtrm / Energy / IoT / "[A leading central-Indian DISCOM]"
2. bridgesense / Infrastructure / Computer Vision / "[State Highway Authority]"
3. salt-lick / Healthcare / AI Platform / "Esperer Bioresearch" (per D-07, named directly)
4. ai-copter / Infrastructure / Drone AI / "[Mining Corporation]"
5. fwa-platform / Insurance / Fraud Detection / "[A Tier-1 public-sector general insurer]" (add TODO: confirm whether to name NIACL publicly)
6. unified-semantic-fabric / Data & Analytics / Semantic Layer / ""
7. bi-acceleration-engine / Data & Analytics / Performance / ""
8. multidimensional-olap-modernization / Data & Analytics / OLAP / ""
9. cloud-analytics-cost-optimization / Data & Analytics / FinOps / ""
10. conversational-data-agent / Data & Analytics / NLP / ""
11. enterprise-reporting-suite / Data & Analytics / Reporting / ""

Tier B entries have client: "" (no client attribution per D-10).
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Eleven systems" app/portfolio/page.tsx
    - grep -q "PortfolioCard" app/portfolio/page.tsx
    - grep -q "Pill" app/portfolio/page.tsx
    - grep -q "activeFilter" app/portfolio/page.tsx
    - grep -q "getAllCaseStudies" app/portfolio/page.tsx
    - test -f content/portfolio/idtrm.mdx
    - test -f content/portfolio/bridgesense.mdx
    - test -f content/portfolio/salt-lick.mdx
    - test -f content/portfolio/ai-copter.mdx
    - test -f content/portfolio/fwa-platform.mdx
    - test -f content/portfolio/unified-semantic-fabric.mdx
    - test -f content/portfolio/enterprise-reporting-suite.mdx
    - ls content/portfolio/*.mdx | wc -l returns 11
    - pnpm build exits with code 0
  </acceptance_criteria>
  <done>
    - Portfolio page renders at /portfolio with hero, sticky filter bar (6 category pills), 3-column card grid
    - Filter pills work: clicking a category shows only matching cards, "All" shows all 11
    - 11 stub MDX files exist in content/portfolio/ with valid Zod-conforming frontmatter
    - Cards appear in correct order per build spec
    - pnpm build succeeds
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- /portfolio route exists with all 11 cards
- Filter pills render and function
- All 11 stub MDX files have valid frontmatter
</verification>

<success_criteria>
Portfolio index page is complete with filtering capability. All 11 stub MDX files provide the data backbone for both this page and the case study detail pages in Plans 07-08.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-06-SUMMARY.md`
</output>
