---
phase: 02-content-pages
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - app/page.tsx
  - content/services.ts
autonomous: true
requirements: [PAGE-01]

must_haves:
  truths:
    - "Home page renders hero with 'AI engineered for the physical world.' headline with 'engineered' in accent"
    - "Capability strip shows 6 ServiceTile components in a 3x2 grid"
    - "Featured case study dark section shows iDTRM with stats and dashboard placeholder"
    - "Portfolio teaser shows 4 PortfolioCard components"
    - "Manifesto band and CTA band render with production copy"
  artifacts:
    - path: "app/page.tsx"
      provides: "Complete home page with all 7 sections"
      contains: "AI engineered for the physical world"
    - path: "content/services.ts"
      provides: "Service metadata for capability strip rendering"
      contains: "AI Agent Development"
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/HeroLight.tsx"
      via: "imports HeroLight for hero section"
      pattern: "import.*HeroLight"
    - from: "app/page.tsx"
      to: "components/cards/ServiceTile.tsx"
      via: "imports ServiceTile for capability strip"
      pattern: "import.*ServiceTile"
    - from: "app/page.tsx"
      to: "components/cards/PortfolioCard.tsx"
      via: "imports PortfolioCard for portfolio teaser"
      pattern: "import.*PortfolioCard"
    - from: "app/page.tsx"
      to: "components/sections/CTABand.tsx"
      via: "imports CTABand for CTA section"
      pattern: "import.*CTABand"
    - from: "app/page.tsx"
      to: "components/sections/ManifestoBand.tsx"
      via: "imports ManifestoBand for manifesto section"
      pattern: "import.*ManifestoBand"
---

<objective>
Build the complete Home page with all 7 sections using components from Wave 1.

Purpose: The home page is the primary entry point for technical buyers. It must convey engineered seriousness and route visitors to Services, Portfolio, and Contact. Every section uses production-ready copy.
Output: Fully functional home page at / with all 7 sections rendering correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-pages/02-CONTEXT.md

Build spec for exact section ordering and copy:
@/Users/george/Downloads/softwires_claude_code_prompt.md (section 4.1 — Home page brief, section 9 — copy guidance)

Design reference:
@DESIGN.md

Prior plan SUMMARYs (needed for component interfaces):
@.planning/phases/02-content-pages/02-01-SUMMARY.md
@.planning/phases/02-content-pages/02-02-SUMMARY.md
@.planning/phases/02-content-pages/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Home page — hero, capability strip, featured case study</name>
  <files>app/page.tsx</files>
  <read_first>
    - /Users/george/Downloads/softwires_claude_code_prompt.md (section 4.1 — full Home page brief)
    - /Users/george/DEV/softwire/DESIGN.md
    - /Users/george/DEV/softwire/app/page.tsx (current content — will be fully replaced)
    - /Users/george/DEV/softwire/components/sections/HeroLight.tsx
    - /Users/george/DEV/softwire/components/sections/HeroDark.tsx
    - /Users/george/DEV/softwire/components/sections/CTABand.tsx
    - /Users/george/DEV/softwire/components/sections/ManifestoBand.tsx
    - /Users/george/DEV/softwire/components/cards/ServiceTile.tsx
    - /Users/george/DEV/softwire/components/cards/PortfolioCard.tsx
    - /Users/george/DEV/softwire/components/ui/StatBlock.tsx
    - /Users/george/DEV/softwire/components/ui/SectionHeader.tsx
    - /Users/george/DEV/softwire/components/layout/Container.tsx
    - /Users/george/DEV/softwire/lib/services.ts
  </read_first>
  <action>
Replace the current app/page.tsx test content with the complete Home page. This is a Server Component.

Export page metadata:
```typescript
export const metadata: Metadata = {
  title: "Softwires Technologies — AI Engineering for the Physical World",
  description: "Softwires builds AI systems that monitor transformers, inspect bridges, screen for cancer, and detect fraud. Deployed in production across India and beyond.",
};
```
Note: Override the template title for home page — use the full brand name as title.

**Section 1: Hero (full viewport, light bg)**
- Use `<HeroLight>` with:
  - headline: "AI engineered for the physical world."
  - highlightWord: "engineered" (per D-09, this word gets accent color)
  - headlineSize: "text-display-xl"
  - intro: "Softwires builds AI systems that monitor transformers, inspect bridges, screen for cancer, and detect fraud — deployed in production across India and beyond."
  - primaryCTA: { label: "See our work", href: "/portfolio" }
  - secondaryCTA: { label: "Talk to engineering", href: "/contact" }
  - showScrollCue: true
- The WebGL particle field placeholder: render a child div inside HeroLight with `className="absolute inset-0 -z-10"` and a comment `{/* TODO: Phase 3 — Three.js particle field (HeroParticleField.tsx), falls back to SVG noise under reduced-motion */}`. For now, leave the background clean.

**Section 2: Capability strip (light bg)**
- Wrap in `<section>` with `py-5xl` padding
- `<Container>` wrapper
- `<SectionHeader kicker="WHAT WE BUILD" heading="Six capabilities. One engineering team." headingSize="text-display-md" />`
- Import SERVICES from lib/services.ts
- Render 6 `<ServiceTile>` components in a grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-xl mt-2xl`
- Each tile receives: title from service.title (short, e.g., "AI Agent Development"), description from service.description, tag from first use case, href to `/services#${service.anchorId}`

**Section 3: Featured case study (DARK section, full-bleed)**
- Wrap in `<section data-theme="dark" className="bg-bg-dark text-text-inverted py-5xl max-md:py-3xl">`
- `<Container>` wrapper
- Caption kicker: "FEATURED \u00B7 ENERGY"
- Display-lg headline: "Real-time monitoring across 8,000 distribution transformers."
  IMPORTANT per D-06 and section 9: The headline says "8,000 distribution transformers" — this is the total fleet being monitored. The system currently runs on 8 DTRs (the installed devices). The headline refers to the transformer population, NOT the DTR count. This is accurate per the source deck.
- 2-column layout (`grid grid-cols-1 lg:grid-cols-2 gap-2xl mt-2xl`):
  - Left: body-lg paragraph (3-4 sentences on iDTRM) describing the system: real-time transformer health monitoring via GPRS-connected DTRs, pre-warning grid failures before consumers notice, deployed across central India's distribution network.
  - Right: placeholder div `className="bg-surface-elevated rounded-xl aspect-video flex items-center justify-center"` with `<span className="text-mono-sm text-text-muted-dark">iDTRM Dashboard — geo-tagged map + live KPI tiles</span>` and `{/* TODO: replace with actual dashboard mock or screenshot */}`
- Stat row below: 4 StatBlock components in a flex row — "5 min" / "Refresh interval", "40 days" / "Load survey retention", "0.5" / "Accuracy class", "24/7" / "Monitoring". All with `dark={true}`.
- CTA: `<Button variant="secondary" href="/portfolio/idtrm" arrow>Read the case study</Button>` with inverted styling for dark section

**Section 4: Portfolio teaser grid (light bg)**
- Wrap in `<section className="py-5xl max-md:py-3xl">`
- `<Container>` wrapper
- `<SectionHeader kicker="RECENT WORK" heading="Systems in production." headingSize="text-display-md" />`
- 4 PortfolioCard components in a grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-xl mt-2xl`
- Cards for: BridgeSense, Salt-Lick, AI-Copter, FWA Platform (hardcoded data — these are the featured 4 per spec)
  - Each card needs: slug, title, category, subcategory, excerpt, heroImage (placeholder path like "/images/portfolio/bridgesense-hero.jpg"), heroImageAlt
  - The images don't exist yet — use placeholder images. Add `{/* TODO: replace placeholder hero images with real photography */}` comment.
  - For the PortfolioCard's next/image, since the actual images don't exist, use a placeholder div with the same aspect ratio and a label instead of a real `<Image>`. The PortfolioCard component should handle a missing image gracefully (render a colored placeholder div).
- Below grid: `<Link href="/portfolio" className="text-body font-medium text-accent hover:text-accent-hover transition-colors duration-fast mt-xl inline-block">See all 11 projects <span aria-hidden="true">&rarr;</span></Link>`

**Section 5: ManifestoBand**
- Render `<ManifestoBand />` with defaults (manifesto copy is baked into the component from Plan 03)

**Section 6: CTABand**
- Render `<CTABand />` with defaults

The page should flow: Hero -> Capability Strip -> Featured Case Study (dark) -> Portfolio Teaser -> Manifesto -> CTA -> Footer (from layout).
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AI engineered for the physical world" app/page.tsx
    - grep -q "engineered" app/page.tsx
    - grep -q "HeroLight" app/page.tsx
    - grep -q "ServiceTile" app/page.tsx
    - grep -q 'data-theme="dark"' app/page.tsx
    - grep -q "PortfolioCard" app/page.tsx
    - grep -q "ManifestoBand" app/page.tsx
    - grep -q "CTABand" app/page.tsx
    - grep -q "WHAT WE BUILD" app/page.tsx
    - grep -q "RECENT WORK" app/page.tsx
    - grep -q "FEATURED" app/page.tsx
    - pnpm build exits with code 0
  </acceptance_criteria>
  <done>
    - Home page renders all 7 sections in correct order: Hero, Capability Strip, Featured Case Study (dark), Portfolio Teaser, Manifesto, CTA Band
    - Hero headline shows "AI engineered for the physical world." with "engineered" in accent color
    - Capability strip renders 6 ServiceTile components from SERVICES data
    - Featured case study is a dark section with iDTRM content and 4 stat blocks
    - Portfolio teaser shows 4 PortfolioCard components
    - pnpm build succeeds
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with no errors
- Home page renders at / with all sections visible
- Dark section has data-theme="dark" for Navbar detection
- All CTAs link to correct pages
</verification>

<success_criteria>
The Home page is complete with production-ready copy, all 7 sections, correct component composition, and proper design token usage. A technical buyer visiting / sees the full landing experience.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-04-SUMMARY.md`
</output>
