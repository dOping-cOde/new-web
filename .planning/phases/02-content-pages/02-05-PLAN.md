---
phase: 02-content-pages
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - app/services/page.tsx
autonomous: true
requirements: [PAGE-02]

must_haves:
  truths:
    - "Services page renders page hero, sticky anchor nav, 6 service sections, engagement model band, and CTA band"
    - "Anchor nav links scroll to corresponding service sections"
    - "Service sections alternate image-left / image-right layout"
    - "Data & Analytics section contains 6 CapabilityCard sub-cards"
  artifacts:
    - path: "app/services/page.tsx"
      provides: "Complete Services page with all sections"
      contains: "Six capabilities"
  key_links:
    - from: "app/services/page.tsx"
      to: "lib/services.ts"
      via: "imports SERVICES metadata"
      pattern: "import.*SERVICES"
    - from: "app/services/page.tsx"
      to: "components/cards/CapabilityCard.tsx"
      via: "imports CapabilityCard for Data & Analytics sub-grid"
      pattern: "import.*CapabilityCard"
---

<objective>
Build the complete Services page with sticky anchor nav and 6 service sections.

Purpose: The Services page is the primary capability overview for technical buyers. It shows all 6 service areas with detailed copy, tech stacks, use cases, and proof-point links to case studies.
Output: Fully functional /services page with anchor navigation, alternating layouts, engagement model band, CTA band.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-pages/02-CONTEXT.md

Build spec for Services page:
@/Users/george/Downloads/softwires_claude_code_prompt.md (section 4.2 — full Services page brief)

Design reference:
@DESIGN.md

Prior plan SUMMARYs:
@.planning/phases/02-content-pages/02-01-SUMMARY.md
@.planning/phases/02-content-pages/02-02-SUMMARY.md
@.planning/phases/02-content-pages/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Services page with anchor nav and 6 service sections</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - /Users/george/Downloads/softwires_claude_code_prompt.md (section 4.2 — full Services page brief)
    - /Users/george/DEV/softwire/DESIGN.md
    - /Users/george/DEV/softwire/lib/services.ts
    - /Users/george/DEV/softwire/components/sections/HeroLight.tsx
    - /Users/george/DEV/softwire/components/sections/CTABand.tsx
    - /Users/george/DEV/softwire/components/cards/CapabilityCard.tsx
    - /Users/george/DEV/softwire/components/ui/Pill.tsx
    - /Users/george/DEV/softwire/components/ui/SectionHeader.tsx
    - /Users/george/DEV/softwire/components/ui/Caption.tsx
    - /Users/george/DEV/softwire/components/layout/Container.tsx
    - /Users/george/DEV/softwire/app/globals.css
  </read_first>
  <action>
Create `app/services/` directory and `page.tsx`.

Export page metadata:
```typescript
export const metadata: Metadata = {
  title: "Services",
  description: "Six AI engineering capabilities — from autonomous agents to grid intelligence. Full-stack AI systems for energy, healthcare, infrastructure, and enterprise data.",
};
```

The Services page is a SERVER COMPONENT for the main structure, but includes a small client component for the sticky anchor nav active state detection. Split the anchor nav into a separate "use client" component inline or as a helper.

**Section 1: Page hero (light)**
- Use `<HeroLight>` with:
  - kicker: "SERVICES"
  - headline: "Six capabilities. One engineering team."
  - headlineSize: "text-display-lg"
  - intro: "We build AI systems end-to-end — from research prototype to production deployment. Each capability is backed by shipped work, not slide decks."

**Section 2: Sticky anchor nav**
- Build as a "use client" component (inline or separate file).
- Sticky below navbar: `position: sticky; top: 64px; z-index: 40` (below navbar z-50)
- Background: `bg-bg-light/95 backdrop-blur-sm`, border-bottom `border-b border-border-light`
- Container: default size
- 6 text links in a horizontal scrollable row (overflow-x-auto on mobile): "AI Agents", "Chatbots", "Healthcare", "Energy", "Infrastructure", "Data & Analytics"
- Each link: `<a href="#ai-agents">AI Agents</a>` with `text-body-sm`, `scroll-behavior: smooth`
- Active state detection: use IntersectionObserver to detect which section is in view. Active link gets `text-accent` color and a 2px indigo underline. This is the client component part — the nav detects which section heading is intersecting.
- On mobile: horizontal scroll with `-webkit-overflow-scrolling: touch`, no wrapping, `gap-xl` between items, `px-xl` padding.

**Section 3: Six service sections** — Each section ~80vh equivalent, alternating image-left / image-right:

For each of the 6 services (import SERVICES from lib/services.ts for tech stacks and metadata), create a `<section id={service.anchorId}>` with:

Layout pattern (alternating per index):
- Odd sections (1, 3, 5): text-left, image/diagram-right
- Even sections (2, 4, 6): image/diagram-left, text-right
- Use `grid grid-cols-1 lg:grid-cols-2 gap-2xl` for the 2-column layout
- `flex-row-reverse` or grid order classes for alternating sides

Content for each section:
- Caption: `<Caption>{service.number} \u00B7 {service.label}</Caption>` (e.g., "01 \u00B7 AI AGENTS")
- Headline: `<h2 className="text-display-md mt-lg">{service.title}</h2>`
- Body: 3 short paragraphs (each ~40-60 words) of production-ready copy. Write these FRESH per the build spec guidance — technical precision voice, concrete verbs, real capabilities.
- Tech stack pills: `<div className="flex flex-wrap gap-sm mt-xl">` with `<Pill as="span">` for each tech in service.techStack
- Use case bullets: 3 bullets in `<ul className="mt-xl space-y-sm">` with `<li className="text-body-sm text-text-muted">` and leading dot
- If service has caseStudySlug: add `<a href={"/portfolio/" + service.caseStudySlug} className="text-body-sm text-accent mt-lg inline-block">Read the case study &rarr;</a>`

Diagram/image side: placeholder div `bg-surface rounded-xl aspect-video flex items-center justify-center border border-border-light` with a descriptive label:
- AI Agents: "Agent loop diagram: perceive > plan > act > verify"
- Chatbots: "Conversational AI architecture"
- Healthcare: "Clinical decision support pipeline"
- Energy: "Grid intelligence monitoring flow"
- Infrastructure: "Structural assessment pipeline"
- Data & Analytics: (no placeholder needed — uses CapabilityCard sub-grid instead)

**SPECIAL: Section 6 — Data & Analytics** (per build spec section 4.2f):
Instead of a placeholder diagram, render 6 `<CapabilityCard>` components in a `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-md` grid:
1. Unified Semantic Fabric — "One source of truth across BI and AI" — tags: ["Semantic Layer", "dbt", "GraphQL"]
2. BI Acceleration Engine — "Sub-second analytics on enterprise datasets at scale" — tags: ["OLAP", "In-Memory", "Aggregation"]
3. Multidimensional OLAP Modernization — "Replace legacy cube engines — SSAS, Essbase, TM1" — tags: ["Cube Migration", "Cloud OLAP", "MDX"]
4. Cloud Analytics Cost Optimization — "Reduce Snowflake/Databricks/BigQuery spend by 40%+" — tags: ["FinOps", "Query Optimization", "Caching"]
5. Conversational Data Agent — "Natural-language analytics on enterprise data" — tags: ["NL-to-SQL", "LLM", "Data Catalog"]
6. Enterprise Reporting Suite — "Unified reporting across business units" — tags: ["Dashboards", "Embedded BI", "Scheduling"]
Each links to `/portfolio/{slug}`.

WRITE FULL PRODUCTION COPY for all 6 service sections. Follow section 9 copy guidance: technical precision, concrete verbs (build, ship, deploy, monitor, predict, detect, route, score), short sentences (avg 14 words), no banned words (leverage, synergy, transform, cutting-edge, best-in-class, seamless, robust, world-class, next-generation, revolutionary, game-changing).

**Section 4: Engagement model band**
- Section: light bg, `py-5xl`, narrow column `max-w-[900px] mx-auto`
- Headline: `<h2 className="text-display-md text-center">How we work.</h2>`
- 3 columns: `grid grid-cols-1 md:grid-cols-3 gap-2xl mt-2xl`
  - Column 1: `<h3 className="text-h3">Discovery</h3>`, `<span className="text-mono-sm text-text-muted block mt-sm">2 weeks</span>`, body-sm paragraph on scoping
  - Column 2: `<h3 className="text-h3">Build</h3>`, `<span className="text-mono-sm text-text-muted block mt-sm">4-12 weeks</span>`, body-sm paragraph on engineering
  - Column 3: `<h3 className="text-h3">Operate</h3>`, `<span className="text-mono-sm text-text-muted block mt-sm">Ongoing</span>`, body-sm paragraph on monitoring and maintenance

**Section 5: CTABand**
- Render `<CTABand />` with defaults

Page flow: Hero -> Anchor Nav -> 6 Service Sections -> Engagement Model -> CTA -> Footer.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Six capabilities" app/services/page.tsx
    - grep -q "SERVICES" app/services/page.tsx
    - grep -q "ai-agents" app/services/page.tsx
    - grep -q "chatbots" app/services/page.tsx
    - grep -q "healthcare" app/services/page.tsx
    - grep -q "energy" app/services/page.tsx
    - grep -q "infrastructure" app/services/page.tsx
    - grep -q "data-analytics" app/services/page.tsx
    - grep -q "CapabilityCard" app/services/page.tsx
    - grep -q "How we work" app/services/page.tsx
    - grep -q "CTABand" app/services/page.tsx
    - grep -q "sticky" app/services/page.tsx
    - pnpm build exits with code 0
  </acceptance_criteria>
  <done>
    - Services page renders at /services with hero, sticky anchor nav, 6 service sections with alternating layouts, engagement model band, CTA band
    - Anchor nav is sticky below navbar with smooth scroll
    - Each service section has caption, headline, 3 paragraphs of production copy, tech pills, use case bullets
    - Data & Analytics section has 6 CapabilityCard sub-cards
    - Engagement model band shows Discovery/Build/Operate with timestamps
    - pnpm build succeeds
  </done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- /services route exists and renders all sections
- All 6 anchor IDs are present in the HTML
- CapabilityCard components render in Data & Analytics section
</verification>

<success_criteria>
Services page is complete with production-ready copy across all 6 service areas, sticky anchor navigation, alternating layouts, and engagement model. A CTO can scroll through the full capabilities overview.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-05-SUMMARY.md`
</output>
