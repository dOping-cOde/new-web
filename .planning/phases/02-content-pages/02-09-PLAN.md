---
phase: 02-content-pages
plan: 09
type: execute
wave: 4
depends_on: [02-04, 02-05, 02-06, 02-07, 02-08]
files_modified:
  - app/about/page.tsx
  - app/contact/page.tsx
  - components/forms/ContactForm.tsx
  - app/not-found.tsx
autonomous: false
requirements: [PAGE-04, PAGE-05, PAGE-06]
must_haves:
  truths:
    - "About page renders hero, origin section, 3 principles, sectors grid, partnerships placeholder, CTA"
    - "Contact page renders hero, validated form, office strip"
    - "Contact form validates required fields and constructs mailto payload"
    - "Custom 404 page has Softwires branding and navigation links"
    - "All 5 top-level pages are reachable via navigation"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "Complete About page with philosophy and principles"
      contains: "Engineers, not sloganeers"
    - path: "app/contact/page.tsx"
      provides: "Complete Contact page with form"
      contains: "Tell us what you"
    - path: "components/forms/ContactForm.tsx"
      provides: "Validated contact form with mailto submission"
      exports: ["ContactForm"]
      contains: "mailto"
    - path: "app/not-found.tsx"
      provides: "Branded 404 page with navigation"
      contains: "Softwires"
  key_links:
    - from: "app/contact/page.tsx"
      to: "components/forms/ContactForm.tsx"
      via: "imports ContactForm"
      pattern: "import.*ContactForm"
    - from: "components/forms/ContactForm.tsx"
      to: "mailto:hello@softwires.in"
      via: "constructs mailto link on submit"
      pattern: "mailto:hello@softwires.in"
---

<objective>
Build About page, Contact page with validated form, and branded 404 page. Then verify all pages work together.

Purpose: About and Contact are the final top-level pages. The contact form is the primary conversion mechanism — it must work flawlessly. The 404 page needs branding. This plan completes all 17 routes.
Output: About page, Contact page with working form, branded 404, all routes verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-pages/02-CONTEXT.md

Build spec for About and Contact pages:
@/Users/george/Downloads/softwires_claude_code_prompt.md (section 4.5 — About, section 4.6 — Contact, section 9 — copy guidance)

Design reference:
@DESIGN.md

Prior plan SUMMARYs:
@.planning/phases/02-content-pages/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build About page and Contact page with form</name>
  <files>app/about/page.tsx, app/contact/page.tsx, components/forms/ContactForm.tsx</files>
  <read_first>
    - /Users/george/Downloads/softwires_claude_code_prompt.md (section 4.5 — About, section 4.6 — Contact)
    - /Users/george/DEV/softwire/DESIGN.md
    - /Users/george/DEV/softwire/.planning/phases/02-content-pages/02-CONTEXT.md (D-13, D-14, D-15)
    - /Users/george/DEV/softwire/components/sections/HeroLight.tsx
    - /Users/george/DEV/softwire/components/sections/CTABand.tsx
    - /Users/george/DEV/softwire/components/ui/SectionHeader.tsx
    - /Users/george/DEV/softwire/components/ui/Caption.tsx
    - /Users/george/DEV/softwire/components/layout/Container.tsx
    - /Users/george/DEV/softwire/app/globals.css
  </read_first>
  <action>
**app/about/page.tsx** — Complete About page (Server Component):

Export metadata:
```typescript
export const metadata: Metadata = {
  title: "About",
  description: "Softwires Technologies — engineers who build AI systems for energy, healthcare, and infrastructure. Not sloganeers.",
};
```

Sections per build spec 4.5:

1. **Hero (LIGHT)** — Use `<HeroLight kicker="ABOUT" headline="Engineers, not sloganeers." headlineSize="text-display-lg" intro="We build AI systems that run in transformers, bridges, hospitals, and mines. Our measure is uptime, not slides." />`

2. **Origin section (LIGHT, narrow column)** — max-w-[720px] mx-auto
   - `<h2 className="text-display-md">Why Softwires exists</h2>`
   - 4 paragraphs (~80 words each) on the founding thesis. Write in technical-precision voice:
     - Paragraph 1: The gap between AI research and physical-world deployment. Labs produce models; the hard part is running them next to 415V transformers and LiDAR rigs.
     - Paragraph 2: India's infrastructure is being rebuilt at unprecedented scale. Smart grids, digital health networks, structural monitoring — each needs AI that works in dust, heat, and unreliable connectivity.
     - Paragraph 3: Softwires exists to bridge that gap. An engineering team that understands both the model and the mounting bracket. Both the neural network and the NABH compliance audit.
     - Paragraph 4: We started with energy (iDTRM), expanded to healthcare (Salt-Lick) and infrastructure (BridgeSense), and built a data platform practice along the way. Each system taught us what the next one needed.

3. **Principles (LIGHT)** — 3 numbered sections, each in a `<section>` with `py-2xl`
   - 01: `<Caption>01</Caption>` + `<h3 className="text-display-md mt-sm">We engineer for the failure mode</h3>` + 2 paragraphs on designing for what goes wrong (power surges, monsoon seasons, network dropouts, not sunny-day demos)
   - 02: `<Caption>02</Caption>` + `<h3 className="text-display-md mt-sm">Latency is a feature, not an afterthought</h3>` + 2 paragraphs on edge inference, on-device processing, sub-second decisions (a transformer overheating, a crack detected, a fraud pattern emerging)
   - 03: `<Caption>03</Caption>` + `<h3 className="text-display-md mt-sm">The model is 10% of the system</h3>` + 2 paragraphs on the 90% that matters: data pipelines, monitoring, retraining schedules, hardware integration, compliance, deployment automation

4. **Sectors grid (LIGHT)** — `<SectionHeader kicker="WHERE WE WORK" heading="Five sectors. Real systems." headingSize="text-display-md" />`
   - 5-tile grid: `grid grid-cols-1 md:grid-cols-3 lg:grid-cols-5 gap-xl mt-2xl`
   - Each tile: `<div className="border border-border-light rounded-md p-lg">` with `<Caption>{sector}</Caption>` and `<p className="text-body-sm text-text-muted mt-sm">{description}</p>`
   - Sectors: Energy ("Grid intelligence and transformer monitoring"), Healthcare ("Cancer screening and clinical decision support"), Infrastructure ("Structural assessment and drone inspection"), Insurance ("Fraud detection and claims automation"), Data Platforms ("Enterprise analytics and semantic layer")

5. **Partnerships band (LIGHT)** — `<SectionHeader heading="Working with leaders across the industry" headingSize="text-display-md" align="center" />`
   - 6 muted-gray placeholder boxes in a grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-xl mt-2xl`
   - Each: `<div className="aspect-[3/1] bg-surface rounded-md border border-border-light flex items-center justify-center"><span className="text-caption text-text-muted">Partner Logo</span></div>`
   - Comment: `{/* TODO: replace with real partner/client logos — see PART-01 */}`

6. **CTA** — `<CTABand />`

**components/forms/ContactForm.tsx** — Validated contact form ("use client" component):

Per D-13 (Claude's discretion on form approach): Use HTML5 native validation + custom styling. No heavy form library — keeps the bundle light and the code straightforward for technical buyers who inspect source.

Per D-14, D-15 for field spec and validation:

```typescript
"use client";

interface FormData {
  name: string;
  email: string;
  company: string;
  role: string;
  sector: string;
  description: string;
  timeline: string;
  budget: string;
}
```

State: `const [formData, setFormData] = useState<FormData>({...defaults})` and `const [errors, setErrors] = useState<Partial<Record<keyof FormData, string>>>({})` and `const [submitted, setSubmitted] = useState(false)`

Fields (per build spec section 4.6):
- Name: `<input type="text" required>` — text-body, border border-border-light, rounded-md, p-md
- Work email: `<input type="email" required>` — same styling
- Company: `<input type="text" required>` — same styling
- Role: `<select>` with options: "" (placeholder "Select your role"), "Founder / CEO", "CTO / Head of Engineering", "Product", "Operations", "Other"
- Sector: `<select>` with options: "" (placeholder "Select your sector"), "Energy", "Healthcare", "Infrastructure", "Insurance", "Other"
- Description: `<textarea rows={4} required placeholder="A brief technical description. What runs today, what needs to change.">` — same styling
- Timeline (optional): `<input type="text" placeholder="e.g., Q3 2026">` — label includes "(optional)"
- Budget range (optional): `<input type="text" placeholder="e.g., $50K-100K">` — label includes "(optional)"

All labels: `<label className="text-body-sm font-medium block mb-sm">`

Error states (per D-15):
- On blur and on submit: validate required fields
- Error message: `<p className="text-body-sm text-accent mt-xs" role="alert" aria-live="polite">{error}</p>`
- Error border: `border-accent` instead of `border-border-light`
- Validation: name min 2 chars, email must match email pattern, company min 2 chars, description min 20 chars

Submit handler:
```typescript
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  if (!validate()) return;

  // Construct mailto payload
  const subject = encodeURIComponent(`New inquiry from ${formData.name} at ${formData.company}`);
  const body = encodeURIComponent(
    `Name: ${formData.name}\nEmail: ${formData.email}\nCompany: ${formData.company}\nRole: ${formData.role}\nSector: ${formData.sector}\n\nDescription:\n${formData.description}\n\nTimeline: ${formData.timeline || "Not specified"}\nBudget: ${formData.budget || "Not specified"}`
  );
  window.location.href = `mailto:hello@softwires.in?subject=${subject}&body=${body}`;
  // TODO: replace with Resend or Formspree integration — see DOC-03
  setSubmitted(true);
};
```

Submit button: `<Button type="submit" variant="primary">Send</Button>` — full width on mobile
After submit: show confirmation message "Your message has been prepared. Your email client should open shortly." in `text-body text-accent` with a checkmark.

Below form: `<p className="text-mono-sm text-text-muted mt-xl text-center">or write directly: <a href="mailto:hello@softwires.in" className="text-accent hover:text-accent-hover transition-colors duration-fast">hello@softwires.in</a></p>`

Form wrapper: `max-w-[640px] mx-auto`

**app/contact/page.tsx** — Contact page (Server Component wrapper + client form):

Export metadata:
```typescript
export const metadata: Metadata = {
  title: "Contact",
  description: "Tell us what you are building. Softwires responds within two business days with technical depth.",
};
```

Sections per build spec 4.6:
1. **Hero** — `<HeroLight kicker="CONTACT" headline="Tell us what you're building." headlineSize="text-display-lg" intro="We respond within two business days. For technical depth, include what runs in production today and what's broken about it." />`
2. **Form** — `<section className="py-5xl max-md:py-3xl"><Container><ContactForm /></Container></section>`
3. **Office strip (LIGHT)** — `<Container>`, 3-column grid:
   - Column 1: `<Caption>ADDRESS</Caption>` + `<p className="text-body-sm text-text-muted mt-sm">[Office address placeholder]</p>` + `{/* TODO: add real office address */}`
   - Column 2: `<Caption>PHONE</Caption>` + `<p className="text-body-sm text-text-muted mt-sm">[Phone placeholder]</p>` + `{/* TODO: add real phone number */}`
   - Column 3: `<Caption>HOURS</Caption>` + `<p className="text-body-sm text-text-muted mt-sm">Monday-Friday, 9:00-18:00 IST</p>`
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Engineers, not sloganeers" app/about/page.tsx
    - grep -q "Why Softwires exists" app/about/page.tsx
    - grep -q "failure mode" app/about/page.tsx
    - grep -q "Partner Logo" app/about/page.tsx
    - grep -q "TODO" app/about/page.tsx
    - grep -q "ContactForm" app/contact/page.tsx
    - grep -q "Tell us what you" app/contact/page.tsx
    - grep -q "mailto:hello@softwires.in" components/forms/ContactForm.tsx
    - grep -q "TODO.*Resend\|TODO.*Formspree" components/forms/ContactForm.tsx
    - grep -q "aria-live" components/forms/ContactForm.tsx
    - grep -q "use client" components/forms/ContactForm.tsx
    - grep -q "required" components/forms/ContactForm.tsx
    - pnpm build exits with code 0
  </acceptance_criteria>
  <done>
    - About page renders all 6 sections with production-ready philosophy copy, 3 principles, 5 sector tiles, partnership placeholders
    - Contact form validates required fields with inline error messages using aria-live="polite"
    - Contact form constructs mailto:hello@softwires.in payload with all form data
    - TODO comment for Resend/Formspree integration present
    - Office strip shows placeholder address/phone with TODO comments
  </done>
</task>

<task type="auto">
  <name>Task 2: Update 404 page with branding and navigation</name>
  <files>app/not-found.tsx</files>
  <read_first>
    - /Users/george/DEV/softwire/app/not-found.tsx (current simple 404)
    - /Users/george/DEV/softwire/DESIGN.md
    - /Users/george/DEV/softwire/components/ui/Button.tsx
    - /Users/george/DEV/softwire/components/layout/Container.tsx
  </read_first>
  <action>
Update the 404 page with Softwires branding and clear navigation back to live pages (per PAGE-06).

Implementation:
- Centered layout, `min-h-[60vh]`, flex column centered
- Container wrapper
- Caption kicker: "404" in `text-text-muted`
- Headline: `<h1 className="text-display-md mt-lg">This page does not exist.</h1>`
- Body: `<p className="text-body text-text-muted mt-lg max-w-[480px] text-center">The URL you followed may be outdated or mistyped. Here are some places to start.</p>`
- Navigation links section: `mt-2xl`, flex column or grid with 4 links:
  - "Home" -> / (using Button secondary)
  - "Services" -> /services
  - "Portfolio" -> /portfolio
  - "Contact" -> /contact
- Each link: `<Button variant="secondary" href={href}>{label}</Button>` in a flex row with `gap-md flex-wrap justify-center`
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "does not exist" app/not-found.tsx
    - grep -q "/portfolio" app/not-found.tsx
    - grep -q "/services" app/not-found.tsx
    - grep -q "/contact" app/not-found.tsx
    - grep -q "Button" app/not-found.tsx
    - pnpm build exits with code 0
  </acceptance_criteria>
  <done>
    - 404 page shows branded error message with navigation to all 4 main sections
    - Uses Button components for navigation links
    - pnpm build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all 17 routes</name>
  <files>none</files>
  <action>
Human verifies all pages render correctly. Run `pnpm dev` and check each route.

Verification steps:
1. http://localhost:3000 — Home page: hero with "engineered" in accent, 6 capability tiles, dark iDTRM section, 4 portfolio cards, manifesto, CTA
2. /services — sticky anchor nav scrolls to correct sections, 6 service areas with copy, Data and Analytics has 6 sub-cards, engagement model band
3. /portfolio — 11 cards display, click each filter pill (Energy, Healthcare, etc.) — cards filter correctly
4. /portfolio/idtrm — dark hero with stats, problem/system/architecture sections, outcomes, up-next navigation
5. /portfolio/unified-semantic-fabric — dark hero, what-it-does, tech stack, use cases, CTA, NO client names
6. /about — origin story, 3 principles, sector grid, partnership placeholders
7. /contact — fill form, test validation (empty required fields show errors), submit opens mailto
8. /nonexistent-page — branded 404 with navigation links
9. Navbar transitions on case study dark heroes
  </action>
  <verify>Human visual inspection</verify>
  <done>All 17 routes render correctly with production copy and working interactions</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds with all 17 routes
- All pages render correctly
- Contact form validates and constructs mailto
- 404 page has proper navigation
- Navbar theme detection works on case study dark heroes
</verification>

<success_criteria>
All 17 routes exist with production-ready content. Every page is navigable, the contact form works, and the entire site flow is coherent. Phase 2 is functionally complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-09-SUMMARY.md`
</output>
