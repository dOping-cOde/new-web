---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified: []
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "pnpm dev starts without errors and pnpm build produces zero TypeScript/ESLint errors"
    - "All design tokens are visible as CSS custom properties in browser devtools"
    - "Fonts load from self-hosted files with no Google Fonts network requests"
    - "Navbar theme switching works on scroll across light/dark sections"
    - "Mobile nav opens/closes correctly with stagger animation"
    - "All components render correctly at mobile, tablet, and desktop breakpoints"
    - "Footer 4-column grid renders on desktop, stacks on mobile"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 1 foundation by running automated checks and presenting the site for human visual verification. This plan validates that all 8 FOUND requirements are met before moving to Phase 2.

Purpose: Catch issues now before 17 pages are built on top of a broken foundation.

Output: Confirmed working foundation ready for Phase 2 content work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification of all foundation requirements</name>
  <files></files>
  <read_first>
    package.json (verify Next.js 15 pin, all dependencies)
    tsconfig.json (verify strict mode settings)
    postcss.config.mjs (verify @tailwindcss/postcss)
    DESIGN.md (verify exists and has complete contents)
    app/globals.css (verify all tokens, @theme, @utility blocks)
    lib/fonts.ts (verify font definitions)
    app/layout.tsx (verify font variables, Navbar, Footer imports)
    components/layout/Navbar.tsx (verify IntersectionObserver, theme detection)
    components/layout/MobileNav.tsx (verify focus trap, stagger)
    components/layout/Footer.tsx (verify 4-column grid, dark bg)
    components/ui/Button.tsx (verify primary/secondary variants)
    components/ui/Pill.tsx (verify active/inactive states)
    components/ui/StatBlock.tsx (verify display-lg accent value)
    components/ui/Caption.tsx (verify text-caption utility)
    components/ui/SectionHeader.tsx (verify kicker + heading + intro)
    components/layout/Container.tsx (verify 1200px/1440px widths)
  </read_first>
  <action>
    Run a comprehensive automated verification suite. Check each FOUND requirement and report results. This task ONLY surfaces failures — it does NOT fix them. Any failures found here must be addressed by re-running the relevant earlier plan (01-01, 01-02, or 01-03).

    **FOUND-01: Project scaffold**
    ```bash
    # Verify Next.js 15 is pinned (exact version, no caret)
    grep '"next"' package.json  # Must show "15.x.y" with no ^ prefix
    
    # Verify TypeScript strict
    grep '"strict": true' tsconfig.json
    grep '"noUncheckedIndexedAccess": true' tsconfig.json
    
    # Verify Tailwind v4 (no JS config)
    test ! -f tailwind.config.ts && test ! -f tailwind.config.js && echo "No JS config - correct"
    grep '@tailwindcss/postcss' postcss.config.mjs
    
    # Verify build passes
    pnpm build
    ```

    **FOUND-02: Design tokens**
    ```bash
    # Verify all 13 color tokens in globals.css
    grep -c 'color-' app/globals.css  # Should be 13+ unique color tokens
    
    # Verify spacing scale
    grep 'spacing-xs: 4px' app/globals.css
    grep 'spacing-5xl: 192px' app/globals.css
    
    # Verify @theme block exists
    grep '@theme {' app/globals.css
    
    # Verify all 11 typography utilities
    for token in text-display-xl text-display-lg text-display-md text-h1 text-h2 text-h3 text-body-lg text-body text-body-sm text-caption text-mono-sm; do
      grep -q "@utility $token" app/globals.css && echo "$token: OK" || echo "$token: MISSING"
    done
    ```

    **FOUND-03: Self-hosted fonts**
    ```bash
    # Verify woff2 files exist locally
    test -f public/fonts/FrauncesVariable.woff2 && echo "Fraunces woff2: OK" || echo "Fraunces woff2: MISSING"
    test -f public/fonts/InterVariable.woff2 && echo "Inter woff2: OK" || echo "Inter woff2: MISSING"
    test -f public/fonts/JetBrainsMonoVariable.woff2 && echo "JetBrainsMono woff2: OK" || echo "JetBrainsMono woff2: MISSING"

    # Verify fonts.ts uses next/font/local (NOT next/font/google)
    grep 'next/font/local' lib/fonts.ts && echo "Uses next/font/local: OK" || echo "FAIL: does not use next/font/local"
    grep 'next/font/google' lib/fonts.ts && echo "FAIL: uses next/font/google" || echo "Does not use next/font/google: OK"

    # Verify font variable names
    grep 'font-fraunces' lib/fonts.ts
    grep 'font-inter' lib/fonts.ts
    ```

    **FOUND-04: DESIGN.md**
    ```bash
    test -f DESIGN.md && echo "DESIGN.md exists" || echo "DESIGN.md MISSING"
    grep 'Design Philosophy' DESIGN.md
    grep '#3D2BFF' DESIGN.md  # accent color
    grep 'Anti-patterns' DESIGN.md
    ```

    **FOUND-05: Navbar**
    ```bash
    grep 'IntersectionObserver' components/layout/Navbar.tsx
    grep 'backdrop-blur' components/layout/Navbar.tsx
    grep 'h-\[64px\]' components/layout/Navbar.tsx
    grep 'data-theme' components/layout/Navbar.tsx
    ```

    **FOUND-06: Footer**
    ```bash
    grep 'bg-bg-dark' components/layout/Footer.tsx
    grep 'grid-cols-4\|lg:grid-cols-4' components/layout/Footer.tsx
    grep 'border-t' components/layout/Footer.tsx
    ```

    **FOUND-07: Core components**
    ```bash
    for component in Button Pill StatBlock Caption SectionHeader Container; do
      file=$(find components -name "$component.tsx" 2>/dev/null | head -1)
      if [ -n "$file" ]; then
        echo "$component: $file exists"
      else
        echo "$component: MISSING"
      fi
    done
    ```

    **FOUND-08: Responsive**
    ```bash
    # Check for responsive breakpoint usage across components
    grep -r 'md:\|lg:\|sm:\|max-sm:' components/ | head -20
    ```

    Report all results. Do NOT attempt to fix any failures — document them so the appropriate earlier plan can be re-run.
  </action>
  <verify>
    <automated>cd /Users/george/DEV/softwire && pnpm build 2>&1 | tail -3 && echo "BUILD: OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits with code 0
    - All 8 FOUND requirement checks pass (see automated checks above)
    - No TypeScript errors in build output
    - No ESLint errors in build output
  </acceptance_criteria>
  <done>All automated verification checks pass for FOUND-01 through FOUND-08. Build is clean. Any failures are documented for the user to address by re-running the relevant earlier plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of complete Phase 1 foundation</name>
  <files></files>
  <action>
    Present the running site for human verification. Start the dev server if not running: `pnpm dev`.

    What was built across Plans 01-03:
    - Project scaffold (Next.js 15, Tailwind v4, TypeScript strict)
    - Design token system (colors, typography, spacing, shadows, motion) as CSS variables and Tailwind @theme
    - Self-hosted fonts (Fraunces, Inter, JetBrains Mono) via next/font/local
    - DESIGN.md at project root
    - Six core UI components (Button, Pill, StatBlock, Caption, SectionHeader, Container)
    - Sticky Navbar with IntersectionObserver theme detection and mobile overlay
    - Dark Footer with 4-column grid

    Human should verify each of 8 checks listed in how-to-verify below.
  </action>
  <what-built>
    Complete Phase 1 foundation: project scaffold (Next.js 15, Tailwind v4), design token system (colors, typography, spacing, shadows, motion), self-hosted fonts (Fraunces, Inter, JetBrains Mono) via next/font/local with .woff2 files in public/fonts/, six core UI components (Button, Pill, StatBlock, Caption, SectionHeader, Container), sticky theme-aware Navbar with mobile overlay, and dark Footer with 4-column grid.
  </what-built>
  <how-to-verify>
    Start the dev server if not already running:
    ```
    cd /Users/george/DEV/softwire && pnpm dev
    ```

    Then verify each item:

    1. **Fonts (FOUND-03):** Open http://localhost:3000 — check that the large hero text uses Fraunces (serif, thin weight) and body text uses Inter (sans-serif). Open DevTools Network tab, filter for "fonts.googleapis.com" and "fonts.gstatic.com" — should show ZERO requests to either domain. Fonts should be served from the local Next.js server only.

    2. **Design tokens (FOUND-02):** Open DevTools, select the html element, check Computed Styles or look at :root CSS variables. Verify:
       - --color-accent = #3D2BFF
       - --spacing-xl = 32px
       - --duration-normal = 320ms

    3. **Typography scale (FOUND-02):** On the homepage, verify the typography scale section shows:
       - display-xl is very large (128px desktop), uses Fraunces thin weight
       - body text is Inter at comfortable reading size
       - mono-sm uses JetBrains Mono
       - Resize to mobile (375px) — sizes should shrink proportionally

    4. **Components (FOUND-07):** Scroll through the component showcase:
       - Buttons: primary (dark bg, hover turns indigo) and secondary (transparent, hairline border)
       - Pills: active (indigo bg) and inactive (transparent, light border)
       - StatBlock: large accent-colored number with small uppercase label
       - Caption: uppercase, letterspaced, muted gray
       - SectionHeader: kicker + large heading + optional intro paragraph
       - Resize to 375px — all components should be readable and properly spaced

    5. **Navbar (FOUND-05):**
       - Stays fixed at top while scrolling
       - Has frosted glass effect (semi-transparent with backdrop blur)
       - Scroll down to the dark section — navbar should smoothly transition to dark colors (~320ms)
       - Scroll back up — navbar transitions back to light

    6. **Mobile nav (FOUND-05, FOUND-08):**
       - Resize to 375px width
       - Hamburger icon appears (two horizontal bars)
       - Tap hamburger — full-screen dark overlay opens
       - Hamburger bars morph into an X shape
       - Links appear with a staggered animation
       - Press Escape — overlay closes
       - Re-open, press Tab — focus cycles within overlay (does not escape to background)

    7. **Footer (FOUND-06):**
       - Scroll to bottom — dark footer with 4 columns (Logo+blurb, Services, Portfolio, Contact)
       - Hairline border at top
       - Copyright and legal links at bottom
       - Resize to 375px — columns stack vertically
       - Resize to 768px — columns show in 2x2 grid

    8. **Responsive (FOUND-08):** Resize the browser through mobile (375px), tablet (768px), and desktop (1280px) — verify nothing overflows, truncates incorrectly, or breaks layout.
  </how-to-verify>
  <verify>Human inspects the running site at http://localhost:3000 and confirms all 8 verification checks pass.</verify>
  <done>Human confirms Phase 1 foundation is visually correct and functionally complete across all breakpoints.</done>
  <resume-signal>Type "approved" to confirm Phase 1 is complete, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. All automated checks from Task 1 pass
2. Human verifies visual correctness in Task 2
3. pnpm build produces clean output with no errors or warnings
</verification>

<success_criteria>
- All 8 FOUND requirements verified (automated + visual)
- No visual regressions or layout issues at any breakpoint
- Navbar theme detection works on scroll
- Mobile nav opens/closes with correct animations and accessibility
- Foundation is ready for Phase 2 content work
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
